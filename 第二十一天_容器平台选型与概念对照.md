# 第二十一天：容器平台选型与概念对照

> 只保留本日必备知识和命令，不收录过程性问题。共享会话目前无法读取，未能核实第 21 天的具体答题与批改，因此不推定个人错题。命令中的中文名称均为占位符，使用前替换。

## 1. 容器化与编排是两层问题

- **容器化**：把程序和依赖打包成镜像，让它在兼容环境中运行。
- **编排与应用托管**：决定在哪里运行、运行几份、如何访问、故障后怎么办、怎样更新版本。

例如，把 C# API 做成镜像，解决了“如何交付程序”；运行两个副本、分发请求、滚动发布，是平台要处理的事。

**有 Dockerfile 不代表需要 Kubernetes；有多个容器也不代表必须选 AKS。**先看实际需要哪些平台能力，再考虑运维成本。

### OCI 解决什么？

OCI 推动镜像、运行时和分发的开放标准，减少不同工具之间的人为兼容障碍。它不是某个云平台，也不是编排器。[OCI FAQ](https://opencontainers.org/faq/)、[OCI 标准概览](https://opencontainers.org/about/overview/)

镜像可移植不等于整套系统可以原封不动搬家：CPU 架构、操作系统、配置、存储、网络和云服务依赖仍要匹配。Compose YAML、Kubernetes YAML、ACA 配置也不是同一种格式。

## 2. 各个平台什么时候用？

| 选择 | 适合做什么 | 需要记住的边界 |
| --- | --- | --- |
| Docker | 本机运行、调试一个容器 | 不自动提供跨机器调度和高可用 |
| Compose | 本地联调 API、数据库、Redis；简单单机部署 | 常规 Compose 不提供 Kubernetes 式多节点调度与故障迁移 |
| ACA | 托管容器 API、微服务、后台消费者和任务 | 平台管理底层，不向你开放底层 Kubernetes API |
| AKS | 需要 Kubernetes API、扩展组件或集群级控制 | 控制更多，也需要承担相应的配置与治理工作 |
| ACI | 按需直接运行一个容器组，例如临时处理任务 | 不是完整应用平台；自动扩缩容、发布管理等需另行解决 |
| App Service | 主要是网站或 Web API，可用代码或容器部署 | 以 Web 应用托管为中心，不必为容器化而迁走 |

ACA 底层使用 Kubernetes，但**使用 ACA 不等于拥有一个可直接操作的 Kubernetes 集群**。AKS 提供 Kubernetes API；AKS Automatic 能代管更多运维工作，也不是所有责任都交给平台。[Azure 容器选型](https://learn.microsoft.com/en-us/azure/container-apps/compare-options)

Compose 不只是开发工具，简单生产部署也能采用；关键是团队能否接受单机故障边界，并自行处理备份、监控和发布。

## 3. 核心概念对照表

这些概念只能按职责比较，不能直接画等号。

| 概念 | 它是什么 | 关键区别 |
| --- | --- | --- |
| Docker container | 由镜像创建的容器实例，可运行或停止 | 是实例，不是镜像本身 |
| Compose service | 一个组件的运行配置，例如 `api`、`db` | 可运行成多个同配置容器，不等于单个容器 |
| Kubernetes Pod | 一起部署、一起调度的一组容器 | 容器共享网络，可共享卷；Pod 可被替换 |
| Kubernetes Deployment | 声明并维护应用副本和更新策略 | 通过 ReplicaSet 管理 Pod，不负责提供固定访问入口 |
| Kubernetes Service | 为一组后端提供稳定发现和访问入口 | 是网络概念，不负责创建或运行 Pod |
| ACA replica | 某个 revision 的运行副本，可包含多个容器 | 更接近 Pod 的运行粒度，不等于版本 |
| ACA revision | App 在某一时刻的版本配置快照 | 版本配置不可原地修改；可以有零个或多个运行副本 |
| ACA app | 被部署、配置和访问的应用资源 | 管理 revision，并提供 ingress、流量与其他应用配置 |
| ACA environment | 一组 App 和 Job 的共同运行边界 | 不是单个 App，也不是交给你管理的 AKS 集群 |

Compose 定义见 [services](https://docs.docker.com/reference/compose-file/services/)；Kubernetes 定义见 [Pod](https://kubernetes.io/docs/concepts/workloads/pods/)、[Deployment](https://kubernetes.io/docs/concepts/workloads/controllers/deployment/)、[Service](https://kubernetes.io/docs/concepts/services-networking/service/)；ACA 定义见 [revision](https://learn.microsoft.com/en-us/azure/container-apps/revisions)、[副本扩缩容](https://learn.microsoft.com/en-us/azure/container-apps/scale-app)、[environment](https://learn.microsoft.com/en-us/azure/container-apps/environment)。

### 用同一个 C# API 理解

- Compose 中有一个 `api` service，可以运行两个容器。
- Kubernetes 中，一个 Deployment 管理两个 Pod；Service 提供稳定访问入口。
- ACA 中，一个 App 的某个 revision 可以运行两个 replica；App 的 ingress 提供访问入口。

因此，**两个 replica 是同一版本的两份运行实例，两个 revision 是两个版本配置**。新旧 revision 可以共存，但不是任何 App 配置变更都会创建新 revision。

尤其别混淆：Compose service 是“组件定义”，Kubernetes Service 是“网络入口”；ACA App 集合了多项应用管理能力，并非某一个 Kubernetes 对象的改名。

## 4. 三个适合 ACA 的场景

以下是根据平台能力归纳的选型示例，不是唯一答案。

| 场景 | 为什么选 ACA |
| --- | --- |
| 小团队部署无状态 C# API，访问量有高峰和低谷 | 需要托管入口、健康检查和按需扩缩容，不需要管理集群 |
| 从消息队列持续取任务的后台消费者 | 根据队列积压扩容，业务逻辑仍使用普通容器程序 |
| 每晚生成报表，或上传文件后处理一次就结束 | 使用 ACA Jobs，支持定时、事件或手动触发，无需一直维持 Web 服务 |

持续处理消息可用 App；“启动、做完、退出”更符合 Job。一次性任务不一定非选 ACI，是否需要平台提供调度和执行管理也很重要。[ACA Jobs](https://learn.microsoft.com/en-us/azure/container-apps/jobs)

缩容到零取决于工作负载、最小副本和缩放规则，不等于所有资源费用归零；需要稳定低延迟时，也要考虑冷启动。

## 5. 三个更适合 AKS 的场景

| 场景 | 为什么更适合 AKS |
| --- | --- |
| 必须运行依赖 CRD／Operator 的现成系统 | 需要 Kubernetes 自定义资源和控制器，而 ACA 不开放底层 Kubernetes API |
| 必须在每个节点部署自定义采集代理，或控制节点级能力 | 需要 DaemonSet、节点池等集群能力；有这类需求时重点评估 AKS Standard 及其限制 |
| 企业已有 Kubernetes 平台规范，必须自行组合网络、存储插件和调度策略 | 需要集群级配置能力，且团队已有相应运维经验 |

这些场景的共同点是“明确需要 Kubernetes 能力”，而不是“项目大”“微服务多”或“听起来更专业”。AKS 也不是可以随意修改托管控制平面主机的虚拟机。[AKS 核心概念](https://learn.microsoft.com/en-us/azure/aks/core-aks-concepts)

选型时先问：**是否需要直接操作 Kubernetes API 或集群级组件？**没有这个需求，优先评估更贴近应用需求的平台；不要默认 Kubernetes 是最终归宿。

## 6. 本日需要掌握的命令

本日以识别对象和比较平台为主，不必创建付费集群。下面只操作已有资源；`-n` 在 Azure CLI 中通常表示资源名字，在 `kubectl` 中表示 namespace。

### Docker 与 Compose：区分实例和服务

```powershell
docker ps -a
docker inspect 容器名字
docker compose -f Compose文件路径 config --services
docker compose -f Compose文件路径 ps -a
docker compose -f Compose文件路径 logs --tail 50 服务名字
```

`config --services` 看服务定义名称，`ps` 看由这些定义创建的容器实例。

### Kubernetes：分别查看运行、副本管理和网络对象

```powershell
kubectl config current-context
kubectl get pods,deployments,services -n 命名空间名字
kubectl describe deployment Deployment名字 -n 命名空间名字
kubectl describe service Service名字 -n 命名空间名字
kubectl logs Pod名字 -n 命名空间名字 --tail=50
```

如果操作已有 AKS，先查看集群，必要时取得访问配置：

```powershell
az aks show -n 集群名字 -g 资源组名字
az aks get-credentials -n 集群名字 -g 资源组名字
kubectl config current-context
```

`get-credentials` 会合并本地 kubeconfig，通常切换当前上下文；它不是创建集群，也不代表自动获得集群管理员权限。执行后确认当前操作目标。[AKS CLI](https://learn.microsoft.com/en-us/cli/azure/aks#az-aks-get-credentials)

### ACA：分别查看环境、应用、版本和副本

```powershell
az containerapp env show -n 环境名字 -g 资源组名字
az containerapp show -n 容器应用名字 -g 资源组名字
az containerapp revision list -n 容器应用名字 -g 资源组名字 -o table
az containerapp replica list -n 容器应用名字 -g 资源组名字 --revision 修订版本名字 -o table
az containerapp logs show -n 容器应用名字 -g 资源组名字 --type console --tail 50
```

这里用 `az containerapp` 管理 ACA，不用 `kubectl` 操作其底层 Pod。副本属于指定 revision，缩到零时没有运行副本是可能的。[ACA 副本命令](https://learn.microsoft.com/en-us/cli/azure/containerapp/replica)

### ACI：识别容器组

```powershell
az container show -n 容器组名字 -g 资源组名字
az container logs -n 容器组名字 -g 资源组名字 --container-name 容器名字
```

`az container` 管理 ACI，`az containerapp` 管理 ACA，不是同一个服务的两种写法。[ACI CLI](https://learn.microsoft.com/en-us/cli/azure/container)

## 7. 本日验收

- [ ] 能解释容器化解决交付，编排／应用托管解决运行管理。
- [ ] 能区分 Compose service 与 Kubernetes Service，以及 ACA revision 与 replica。
- [ ] 能说出三个适合 ACA、三个更适合 AKS 的场景，并给出能力上的理由。
- [ ] 知道 ACI、Compose、App Service 各自也有合理用途，不把所有场景都放进 Kubernetes。
- [ ] 知道 OCI 提升互操作性，但不保证操作系统、架构和平台配置自动兼容。
