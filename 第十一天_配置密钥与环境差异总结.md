# 第十一天：配置、密钥与环境差异

> 学习目标：同一份镜像能用于开发、测试、生产；环境差异在运行时注入；密码、令牌等密钥不进入 Dockerfile、镜像层、Git 或普通日志。

## 一、先分清三类东西

| 类别 | 什么时候使用 | 例子 | 能否放密钥 |
|---|---|---|---|
| 构建时参数 | 制作镜像时 | 版本号、构建模式 | 不要 |
| 运行时配置 | 容器启动时 | 数据库地址、端口、日志级别 | 尽量不放密码 |
| 密钥 | 构建或运行时临时授权 | 密码、Token、私有 NuGet 凭据 | 可以，但要用 Secret |

```text
Dockerfile / build
    └─ 决定镜像里有什么

Compose / docker run
    └─ 决定这个环境下怎样运行

Secret
    └─ 只在确实需要时临时提供敏感值
```

最实用的判断方法：

- 所有环境都一样的内容，才适合进入镜像。
- 地址、端口、环境名等会变化的内容，在运行时配置。
- 密码、令牌、私钥，无论何时都不能写死在镜像或 Git 中。

## 二、为什么不能把密码写进镜像

下面这些做法都不安全：

```dockerfile
ENV DB_PASSWORD=real-password
ARG NUGET_TOKEN=real-token
RUN dotnet nuget add source ... --password real-password
```

原因：

- `ENV` 会成为镜像配置的一部分。
- `ARG` 或 `RUN` 中用过的敏感值可能出现在构建记录、缓存或镜像历史中。
- 镜像可被导出、推送、共享；一旦泄漏很难彻底收回。
- 密钥写进仓库、`.env`、截图或普通日志，都会扩大泄漏范围。

`docker history --no-trunc` 能显示镜像层的构建命令，是本次验收的基础检查，但它不是唯一的安全证明。

## 三、`ARG`、`ENV`、Compose `environment`

| 写法 | 生命周期 | 典型用途 | 关键提醒 |
|---|---|---|---|
| Dockerfile `ARG` | 仅构建阶段 | 版本、构建开关 | 不传密码 |
| Dockerfile `ENV` | 镜像默认值 + 容器运行时 | 默认端口、默认时区 | 不能放环境专属密码 |
| Compose `environment` | 本次容器运行 | 地址、端口、连接字符串的非敏感部分 | 可覆盖镜像中的 `ENV` |

```dockerfile
ARG BUILD_CONFIGURATION=Release
ENV ASPNETCORE_HTTP_PORTS=8080
```

```yaml
services:
  api:
    environment:
      ASPNETCORE_HTTP_PORTS: "8080"
      Logging__LogLevel__Default: Information
```

`ARG` 的重点是“构建”；`environment` 的重点是“运行”。不要因为两者都叫变量，就把它们当作密钥通道。

## 四、构建密钥：为什么 `--secret` 和 `RUN --mount` 都要写

这是本次会话中需要纠正的重点。

```text
docker build --secret ...
        ↓ 提供密钥来源
BuildKit
        ↓
RUN --mount=type=secret ...
        ↓ 在这一条 RUN 中临时使用
```

两者不是重复，而是两个步骤：

| 位置 | 作用 |
|---|---|
| 构建命令的 `--secret id=...,src=...` | 把宿主机上的密钥交给本次构建 |
| Dockerfile 的 `RUN --mount=type=secret,id=...` | 声明哪一条 `RUN` 需要以哪个 ID 使用它 |

ID 必须对应：

```dockerfile
# syntax=docker/dockerfile:1
RUN --mount=type=secret,id=nuget_config,required=true \
    dotnet restore --configfile /run/secrets/nuget_config
```

```powershell
docker build `
  --secret id=nuget_config,src=./secrets/NuGet.Config `
  -t portableorders-api:local .
```

密钥默认以临时文件形式挂载到 `/run/secrets/<id>`，该 `RUN` 结束后不会保存进镜像层。前提是：**不要把它 `COPY`、`cat` 到输出文件或打印到日志。**

构建密钥适合访问私有包源、私有 Git 仓库或构建期 API；应用启动后还需要的数据库密码，必须另外用运行时 Secret 提供。

## 五、运行时密钥：Compose Secret

Compose Secret 按服务授权，容器内通常以文件形式出现在：

```text
/run/secrets/<secret_name>
```

示例：

```yaml
services:
  api:
    build: .
    environment:
      ConnectionStrings__DefaultConnection: "Host=db;Port=5432;Database=orders;Username=app"
    secrets:
      - db_password

  db:
    image: postgres:18
    environment:
      POSTGRES_DB: orders
      POSTGRES_USER: app
      POSTGRES_PASSWORD_FILE: /run/secrets/db_password
    secrets:
      - db_password

secrets:
  db_password:
    file: ./secrets/db_password.txt
```

- `secrets` 顶层：声明密钥来自哪里。
- 服务内 `secrets`：把密钥授予特定服务。
- `POSTGRES_PASSWORD_FILE` 是部分官方镜像支持的约定；它会从文件读取密码。
- ASP.NET Core 不会自动把任意 `/run/secrets/*` 文件读入配置，需要应用显式读取，或使用适合的配置提供程序。

例如在应用启动时读取密码文件，再组合连接字符串：

```csharp
var passwordPath = "/run/secrets/db_password";
if (File.Exists(passwordPath))
{
    var password = File.ReadAllText(passwordPath).Trim();
    var baseConnection = builder.Configuration
        .GetConnectionString("DefaultConnection");
    builder.Configuration["ConnectionStrings:DefaultConnection"] =
        $"{baseConnection};Password={password}";
}
```

不要把最终连接字符串或 `password` 输出到日志。

### 本地开发的简化做法

如果暂时不使用 Compose Secret，可将密码放在**未提交**的 `.env` 或本地文件中，再由 Compose 注入。它比写死在 YAML 好，但环境变量仍可能被调试输出或有宿主机权限的人看到，因此生产更推荐 Secret。

```gitignore
.env
.env.*
!.env.example
secrets/
```

提交 `env.example`，只保留变量名和示例值：

```dotenv
DB_HOST=db
DB_PASSWORD=replace-me
```

## 六、`.env`、`env_file`、`environment` 不一样

| 配置 | 主要作用 | 是否自动进入容器 |
|---|---|---|
| `.env` | 替换 Compose 中的 `${变量}` | 否 |
| `env_file` | 从文件把变量传入容器 | 是 |
| `environment` | 在 YAML 中直接把变量传入容器 | 是 |

```dotenv
# .env（给 Compose 读取）
DB_HOST=db
```

```yaml
services:
  api:
    environment:
      ConnectionStrings__DefaultConnection: "Host=${DB_HOST};Port=5432;..."
```

过程是：

```text
.env 提供 DB_HOST
        ↓
Compose 替换 ${DB_HOST}
        ↓
environment 把结果传给容器
```

检查实际生效的 Compose 配置：

```powershell
docker compose config
docker compose config --environment
```

## 七、ASP.NET Core 配置与双下划线

ASP.NET Core 使用 `:` 表示层级；环境变量中应使用 `__`，框架会自动转换。

| C# / JSON 配置键 | 环境变量写法 |
|---|---|
| `ConnectionStrings:DefaultConnection` | `ConnectionStrings__DefaultConnection` |
| `Logging:LogLevel:Default` | `Logging__LogLevel__Default` |
| `Jwt:Issuer` | `Jwt__Issuer` |

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  }
}
```

```yaml
environment:
  Logging__LogLevel__Default: Warning
```

默认应用配置中，后读取的值会覆盖前面的同名值。常见优先级可先记为：

```text
appsettings.json
  < appsettings.{Environment}.json
  < User Secrets（开发）
  < 环境变量
  < 命令行参数
```

因此镜像可以保留不敏感默认值，Compose 或平台在运行时覆盖环境差异。

## 八、让镜像不携带环境专属配置

镜像中只保留应用代码和安全的默认配置，不包含真实密码、开发环境文件或本地 Secret 文件。

`.dockerignore` 示例：

```dockerignore
.env
.env.*
!.env.example
secrets/
appsettings.Development.json
appsettings.Production.json
```

如果项目必须保留这些文件在源码目录，可在项目文件中阻止它们进入发布目录：

```xml
<ItemGroup>
  <Content Update="appsettings.Development.json"
           CopyToPublishDirectory="Never" />
</ItemGroup>
```

不要把真实配置直接放进 `appsettings.Production.json` 再发布镜像；生产差异应该由部署环境提供。

## 九、实践与验收

### 1. 清理镜像中的环境专属信息

- [ ] Dockerfile 中没有密码、Token、私有连接字符串。
- [ ] `.dockerignore` 排除了 `.env` 和 `secrets/`。
- [ ] 发布产物中不含环境专属 `appsettings.*.json`。
- [ ] 连接地址、日志级别、环境名由运行时注入。

### 2. 数据库配置分开注入

```text
数据库地址 / 端口 / 库名 / 用户名 → environment
数据库密码                         → Compose Secret 或未提交本地文件
```

API 在 Compose 网络中应使用服务名，例如：

```text
Host=db;Port=5432;Database=orders;Username=app
```

不要使用 `localhost`；它表示 API 容器自身。

### 3. 检查镜像历史

```powershell
docker history --no-trunc portableorders-api:local
docker image inspect portableorders-api:local
```

检查输出中不应出现：

- 实际密码、Token、私有 NuGet 源凭据。
- `ENV DB_PASSWORD=...`。
- 含真实密钥的 `RUN`、`ARG` 命令。

如果曾经把密钥构建进镜像，即使后来 Dockerfile 删除了那一行，旧镜像和已推送的版本仍可能含有它；应删除旧镜像、撤销或轮换该密钥。

## 十、本次错题与易混点

| 问题 | 正确理解 |
|---|---|
| Dockerfile 已写 `RUN --mount=type=secret`，为什么构建还要 `--secret`？ | 前者声明“在哪一步使用哪个 ID”，后者提供“该 ID 对应的密钥内容”；这是必须配合的两步。 |
| `ARG` 能传变量，能否传密码？ | 技术上可传，但不应这样做；构建参数和环境变量不适合密钥。 |
| `.env` 写了变量，容器一定能读到吗？ | 不一定；`.env` 默认是 Compose 插值来源，需通过 `environment` 或 `env_file` 才进入容器。 |
| `ConnectionStrings:DefaultConnection` 能直接作为环境变量名吗？ | 跨平台应写为 `ConnectionStrings__DefaultConnection`。 |
| Secret mount 是否天然保证绝不泄漏？ | 它不会自动进入镜像层；但仍不能把秘密复制到镜像、写入产物或打印到日志。 |

## 十一、最终检查清单

- [ ] 能区分构建时参数、运行时配置和密钥。
- [ ] 知道密码不能写进 Dockerfile、镜像层、Git 或日志。
- [ ] 能解释 `--secret` 与 `RUN --mount=type=secret` 的配合关系。
- [ ] 知道构建 Secret 和运行时 Secret 是两条不同路径。
- [ ] 能区分 `.env`、`env_file`、`environment`。
- [ ] 能把 `A:B:C` 写为环境变量 `A__B__C`。
- [ ] 已将数据库地址与密码分开注入。
- [ ] 已执行 `docker history --no-trunc`，确认没有密钥痕迹。

## 参考资料

- [Dockerfile: `RUN --mount=type=secret`](https://docs.docker.com/reference/dockerfile/#run---mounttypesecret)
- [Docker Build secrets](https://docs.docker.com/build/building/secrets/)
- [ASP.NET Core configuration](https://learn.microsoft.com/en-us/aspnet/core/fundamentals/configuration/)
- [Docker Compose environment variables](https://docs.docker.com/compose/how-tos/environment-variables/)
- [Docker Compose secrets](https://docs.docker.com/compose/how-tos/use-secrets/)
- [docker image history](https://docs.docker.com/reference/cli/docker/image/history/)
