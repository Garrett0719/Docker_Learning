# 第十九天：探针、资源请求与限制

> 根据官方资料、本地项目，以及共享会话中已核实的健康检查题、第 9、10 题批改整理。其余题目未能完整展开，不推定对错。仅收录本日内容；下面是实验步骤，不代表已替你执行或验证通过。

## 1. 先记住三类探针的分工

| 探针 | 问的是什么 | 连续失败达到阈值后 |
| --- | --- | --- |
| startup | 应用启动完成了吗？ | 终止容器，按重启策略处理 |
| liveness | 应用是否卡死到需要重启？ | 终止容器，按重启策略处理 |
| readiness | 现在能接业务请求吗？ | 标记未就绪，不因这一项失败而重启 |

- 配置 startup 后，它成功之前不会执行 liveness、readiness；每次容器重新启动，startup 都重新检查。
- startup 成功后，本次运行不再继续检查 startup；另外两类持续工作。
- readiness 和 liveness 不是先后阶段。**应用可以活着，但暂时不能接流量。**
- 对你使用的 Deployment，容器通常在原 Pod 内重启，不是每次失败都创建新 Pod。

例如数据库暂时断开：API 进程可能仍正常，readiness 失败、liveness 成功才合理。普通 Service 不再向未就绪 Pod 分发新流量，但不代表现有连接会立即断开，也不阻止直接访问 Pod IP。[三类探针](https://kubernetes.io/docs/concepts/workloads/pods/probes/)

## 2. 你的 YAML 应该怎么看？

本地 `deployment.yaml` 已有两个副本，下面是每个 API 容器的配置片段：

```yaml
resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"
startupProbe:
  httpGet: { path: /health/startup, port: 8080 }
  periodSeconds: 2
  timeoutSeconds: 1
  failureThreshold: 15
livenessProbe:
  httpGet: { path: /health/live, port: 8080 }
  periodSeconds: 5
  timeoutSeconds: 2
  failureThreshold: 3
readinessProbe:
  httpGet: { path: /health/ready, port: 8080 }
  periodSeconds: 3
  timeoutSeconds: 2
  failureThreshold: 3
```

| 字段 | 通俗解释 |
| --- | --- |
| `periodSeconds` | 多久检查一次 |
| `timeoutSeconds` | 一次检查最多等多久 |
| `failureThreshold` | 连续失败几次才采取对应措施 |
| `initialDelaySeconds` | 首次检查前等待多久；默认 0 |
| `successThreshold` | 失败后连续成功几次才算恢复；默认 1，startup/liveness 必须为 1 |

startup 的启动预算可粗略按 `2 × 15 ≈ 30 秒` 理解，不是精确到秒的重启时间；实际还受探测时机、超时和终止宽限期影响。

HTTP 探针由节点上的 kubelet 发起，通常直接访问 `PodIP:8080`，不是走 Service 的 `80` 端口。常规 HTTP 响应 `200～399` 算成功；写错路径返回 `404` 就会失败。API 必须监听可从 Pod 网络访问的地址，不能只监听容器内的 `localhost`。[探针配置与实验](https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/)

## 3. request、limit、throttling、OOM

| 配置 | 作用 | 不要理解成 |
| --- | --- | --- |
| CPU request | 调度依据；CPU 争抢时影响分配权重 | 实际用量或最高用量 |
| 内存 request | 调度时为工作负载计算资源需求 | 启动时立即占用这么多内存 |
| CPU limit | 限制可用 CPU 时间，超出需求会被限速 | 超过就直接杀死容器 |
| 内存 limit | 内存边界，超限可能触发 OOM 杀进程 | 像 CPU 一样等一等就能继续 |

`1000m = 1 CPU`，`500m = 0.5 CPU`，不是“整台机器的 50%”。`128Mi` 是内存容量，别把 `Mi` 写成 CPU 使用的 `m`。

你的配置表示：每个 API 容器 request 为 `100m / 128Mi`，limit 为 `500m / 512Mi`。两个这样的单容器 Pod，容器 request 合计为 `200m / 256Mi`，并非它们现在已经使用了这些资源。

调度主要看 request 能否放入节点的可分配资源，而不是只看节点此刻 CPU 利用率。request 太大可能导致 `Pending + FailedScheduling`；太小则可能低估运行需求。[资源管理](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/)

CPU 限速可以理解成：**工作还在，但本轮可用 CPU 时间用完了，需要等。**它可能让接口和探针变慢，却不直接等于 OOM 或重启。[CPU 资源实验](https://kubernetes.io/docs/tasks/configure-pod-container/assign-cpu-resource/)

## 4. C# 端点：路径不同，检查内容也要合理区分

你当前代码只有 `AddHealthChecks()`，没有注册数据库、Redis 等检查：

- `/health/startup` 和 `/health/live` 的 `Predicate = _ => false`：跳过所有已注册检查，不是“返回失败”。
- `/health/ready` 默认执行全部已注册检查；现在没有注册项，因此端点能正常响应时也会报告健康。
- 所以当前代码能做探针机制实验，**不能证明数据库可用或业务完全就绪**。

将来加入必需的数据库检查，可以用标签区分。下面是结构示例，`DatabaseHealthCheck` 需要自己实现，不能直接当作已有类型：

```csharp
// 在 builder.Build() 之前注册。
builder.Services.AddHealthChecks()
    .AddCheck<DatabaseHealthCheck>("database", tags: new[] { "ready" });

// 在 app.Run() 之前映射。
app.MapHealthChecks("/health/ready", new HealthCheckOptions
{
    Predicate = check => check.Tags.Contains("ready")
});
app.MapHealthChecks("/health/live", new HealthCheckOptions
{
    Predicate = _ => false
});
```

readiness 只检查真正影响接流量的条件，不要把所有可选外部服务都绑进去。检查要快、有超时；数据库可以检查连接，必要时执行 `SELECT 1`，不要跑全表统计。若初始化在后台进行，startup/ready 还需要反映真实初始化状态，不能只看 HTTP 已经能响应。[ASP.NET Core 健康检查](https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/health-checks)

### 重启风暴是怎么发生的？

```text
CPU limit 太紧，或探针做昂贵查询
→ 响应变慢 → liveness 超时 → 容器重启
→ 剩余副本接更多流量 → 更慢 → 更多重启
```

同样，把数据库临时故障放进 liveness，也可能引发集体重启；重启 API 并不能修好数据库。不要只靠增大超时掩盖问题，应同时检查探针逻辑、资源边界和实际负载。

## 5. 已核实的错题与回答不完整处

依据：[共享问答记录](https://chatgpt.com/share/6a8fed97-be4c-83ee-bd9e-e30d2bd2ba36)。这里区分“说错”和“遗漏”，不是把整题都判错。

### ① 健康检查题：不是“成功后进入 live 状态”

你已经理解了“只拆 URL、不拆检查逻辑”会出问题；需要改正的是最后的状态名称：

**readiness 成功，使容器就绪；其他条件满足后，Pod 为 Ready，可正常接收 Service 流量。**liveness 不负责授予接流量资格，`Live ✅ / Ready ❌` 完全可能。

### ② 第 9 题：CPU request 不只用于选择 Node

你回答的调度作用是对的，但漏了 CPU 争抢时的权重作用。

题目中 A request 为 `500m`，B 为 `100m`，两者 limit 都是 `1000m`：

- 资源空闲时，两者都可以超过自己的 request，仍受各自 limit 约束。
- CPU 紧张、两者都忙时，A 通常获得更高的分配权重。
- 不是 A 永远只能用 `500m`，也不是任何时刻都能保证严格的用量比例。

记法：**request 看调度和争用，limit 看上限。**[CPU request 与权重的官方说明](https://kubernetes.io/blog/2026/01/30/new-cgroup-v1-to-v2-cpu-conversion-formula/)

### ③ 第 10 题：没写 request，不一定等于没有 request

题目只写了 limit：`cpu: 100m`、`memory: 128Mi`。你据此判断可能因为没 request 而被随意放到资源不足的 Node，需要纠正。

**如果没有其他准入机制提供默认 request，Kubernetes 会把对应 limit 复制为 request。**本题通常等效于 request 也是 `100m / 128Mi`，应该检查实际 Pod 配置，不能只凭原始 YAML 判断。[默认行为](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#requests-and-limits)

你识别出的“CPU 限速可能让 liveness 超时”是合理假设，但不是仅凭超时就能证明：

- `OOMKilled`：证明有过 OOM 终止，要查内存峰值、limit 和泄漏可能。
- `Liveness probe failed` 加 `Killing` 事件：支持探针触发终止，要核对时间。
- `RESTARTS = 8`：不能认定 8 次都由同一个原因造成。
- `Last State` 主要反映上一次终止，事件也会过期或合并；完整历史需要持续监控。

## 6. 实践与验收命令（PowerShell）

只在本地学习集群做故障实验，每次只改一个变量。下面沿用你的 `study-security` 和 `oom-demo.yaml`；不会要求把 OOM 故障代码加进 API。

### ① 建立正常基线

```powershell
Set-Location 'D:\Docker_Learning\study_security\study_security'
kubectl config current-context
$ns = 'default' # 换成实际 namespace
kubectl apply -n $ns -f deployment.yaml
kubectl rollout status -n $ns deployment/study-security --timeout=180s
kubectl get pods -n $ns -l app=study-security -o wide
```

前提：集群能使用 `study-security:v3` 镜像，ConfigMap 和 Secret 已存在。记录每个 Pod 的名称、READY、RESTARTS，后面都和这个基线比较。

### ② 故意填错路径，再恢复

用编辑器只把 `readinessProbe.httpGet.path` 改为 `/health/not-found`，保存后：

```powershell
kubectl apply -n $ns -f deployment.yaml
kubectl get pods -n $ns -l app=study-security -w
```

`-w` 持续观察，按 `Ctrl+C` 退出。另一个终端选中本次发布的新 Pod：

```powershell
$podName = '替换为新Pod名称'
kubectl describe pod $podName -n $ns
kubectl get events -n $ns --field-selector "involvedObject.name=$podName" --sort-by=.metadata.creationTimestamp
```

预期：startup 通过后，readiness 出现 `404`，新 Pod 未就绪，但不会仅因为 readiness 失败而增加重启次数。滚动发布可能保留旧的健康 Pod，所以“API 还能访问”不代表新探针配置正确。

恢复 `/health/ready` 后重新 apply，并等待 `rollout status` 成功。想比较重启行为，可再单独把 liveness 路径填错：startup 成功后连续 liveness 失败会导致容器重启；观察后立刻恢复。

### ③ 启动延迟实验（与 OOM 二选一即可）

你的程序在 `app.Run()` 前等待 `STARTUP_DELAY_SECONDS`，等待期间尚未开始监听 HTTP：

- 当前 `20` 秒：通常能在约 30 秒的 startup 预算内启动。
- 改成 `45` 秒：预计启动检查先失败到阈值，容器反复被终止，无法完成启动；此时不要误判成 liveness 问题。

修改 `configmap.yaml` 后执行：

```powershell
kubectl apply -n $ns -f configmap.yaml
kubectl rollout restart -n $ns deployment/study-security
kubectl get pods -n $ns -l app=study-security -w
```

通过 `describe pod` 确认是 `Startup probe failed`，通过日志确认启动延迟。实验后把延迟恢复为 `20`，重复 apply、restart，再等待发布完成。若真实业务确实需要 45 秒，应按测量结果增加 startup 预算并留余量。

### ④ OOM 实验（可选）

你已有的 `oom-demo.yaml` 使用独立 BusyBox Pod，不断扩大字符串，内存 request 为 `8Mi`、limit 为 `32Mi`。

```powershell
kubectl apply -n $ns -f oom-demo.yaml
kubectl get pod oom-demo -n $ns -w
```

观察到重启后，退出观察并保存证据：

```powershell
kubectl describe pod oom-demo -n $ns
kubectl logs oom-demo -n $ns --previous --tail=30
```

验收看 `Last State → Terminated → Reason: OOMKilled`；`Exit Code: 137` 常见，但**单独看到 137 不能证明 OOM**。这个 Pod 没有配置探针，如果确认为 OOM，就不是探针导致的重启。`restartPolicy: Always` 会让它继续运行并可能再次 OOM。[内存超限实验](https://kubernetes.io/docs/tasks/configure-pod-container/assign-memory-resource/)

记录后结束实验，删除的只是临时 Pod，不删除本地 YAML：

```powershell
kubectl delete pod oom-demo -n $ns
```

## 7. 排障速查：先找证据，别只看 STATUS

| 现象 | 优先检查 |
| --- | --- |
| `Pending` | Events；若是 `FailedScheduling / Insufficient ...`，再查 request 与节点可分配资源 |
| `Running` 但 `READY 0/1` | startup 是否完成、readiness 是否通过；Running 不等于可接流量 |
| `OOMKilled` | 内存限制、历史峰值、异常增长 |
| `Liveness probe failed` | 路径、端口、耗时、CPU 限速；失败是否已达到重启阈值 |
| `CrashLoopBackOff` | 反复失败后的退避等待，不是根因；查上次退出原因和日志 |

常用补充命令：

```powershell
kubectl logs $podName -n $ns --tail=100
kubectl logs $podName -n $ns --previous --tail=100
kubectl top pod -n $ns --containers
kubectl describe node NODE_NAME
```

`--previous` 看同一 Pod 中上一次容器实例的日志；`NODE_NAME` 换成实际节点名。`kubectl top` 需要 Metrics Server，只反映近期用量，可能错过 OOM 瞬间，也不能单独证明 CPU throttling；后者应结合监控中的 CPU 限速指标。[Pod 生命周期](https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/)、[kubectl top](https://kubernetes.io/docs/reference/kubectl/generated/kubectl_top/)

完成标准：能用“改了什么 → 看到什么证据 → 谁采取了什么动作 → 如何恢复”解释一次探针故障，以及一次启动延迟或 OOM 实验；最后 API 恢复就绪，故障配置不留在正常部署中。
