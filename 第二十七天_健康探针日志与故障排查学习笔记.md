# 第27天学习笔记：健康探针、日志和故障排查

> 根据学习资料及26道验收题整理。第2题已说明是看错选项，不计为知识薄弱点。本文只保留可复用的知识、命令和诊断方法。

## 1. 核心知识点

### 1.1 三种探针，决定三种不同的处理方式

| 探针 | 回答的问题 | 失败意味着什么 |
| --- | --- | --- |
| Startup | 应用启动完成了吗？ | 继续等待；连续失败达到阈值后触发重启 |
| Liveness | 应用是否还能正常响应？ | 连续失败达到阈值后触发重启 |
| Readiness | 当前副本适合接收请求吗？ | 不应向该副本分配正常业务流量 |

**容易混淆：**

你把 Readiness 主要理解为“初始化是否完成”，范围偏窄。它会在运行期间持续判断：**初始化完成后，也可能再次变得不就绪。**

例如数据库暂时不可用：

```text
应用自身正常 → Liveness 可以成功
无法完成核心业务 → Readiness 可以失败
```

Readiness 失败本身不能证明进程崩溃，不应把它直接等同于 Liveness 的重启判断。

### 1.2 为什么不把三种探针写成同一套检查？

重点不是“方便管理”，而是**失败后希望平台采取的动作不同**。

- Startup：检查必要初始化是否完成。
- Liveness：检查应用自身是否卡死、失去响应。
- Readiness：检查此刻是否具备接业务请求的条件。

**容易混淆：**

你回答过“分开方便管理”，方向对，但还应说明：如果把数据库连通性也作为 Liveness 的必要条件，数据库故障可能导致所有 API 反复重启，而重启 API 修不好数据库。

依赖检查应轻量、有超时，只检查影响当前服务能力的必要依赖，不执行昂贵查询或修改数据。[ASP.NET Core 健康检查](https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/health-checks)

### 1.3 ACA 支持什么探针？

- 支持 **HTTP、HTTPS、TCP**。
- HTTP(S) 状态码 `200～399` 表示成功，其他状态码表示失败。
- TCP 只检查能否建立连接，不验证业务接口。
- 每个容器每种探针最多一个。
- 不支持 `exec`、原生 gRPC 探针；端口必须使用整数。

**容易混淆：**

你漏答了 HTTPS。另一个重要区别是：

```text
TCP 成功 → 有程序接受连接
HTTP 健康端点成功 → 通过了端点实际实现的检查
```

两者都不能自动证明所有业务功能正常。[ACA 探针支持与限制](https://learn.microsoft.com/en-us/azure/container-apps/health-probes)

### 1.4 默认探针不能替代业务健康检查

通过 Portal 启用 ingress、且没有定义相应探针时，Portal 会给主应用容器添加默认探针，使用 **TCP 和 ingress target port**。

需要知道：

- 不要认为 CLI、YAML 的所有部署路径都一定替你添加相同配置，应查看实际模板。
- Portal 不自动给 sidecar 添加这些默认探针。
- GPU workload profile 存在默认探针例外。
- 默认 TCP 探针无法检查数据库权限、业务初始化等逻辑。

生产应用应按实际需要配置明确的健康端点，不必死背全部默认时间值。[默认探针说明](https://learn.microsoft.com/en-us/azure/container-apps/health-probes#default-configuration)

### 1.5 探针时间参数：给启动留够时间

| 参数 | 含义 |
| --- | --- |
| `initialDelaySeconds` | 开始检查前等待多久 |
| `periodSeconds` | 大约多久检查一次 |
| `timeoutSeconds` | 单次检查最多等待多久 |
| `failureThreshold` | 连续失败多少次后判定失败 |

Startup 的检查预算可粗略按：

```text
初始等待时间 + 检查间隔 × 失败阈值
```

估算，实际还受执行时间和调度影响。

你已经正确理解：启动时间长于探针允许时间，可能造成“还没启动完就重启”的循环。

**原则：**先确认是启动慢还是程序错误，再调整时间；不能靠无限放宽探针掩盖故障。

### 1.6 应用监听端口、Probe port、targetPort

| 配置 | 作用 |
| --- | --- |
| 应用监听端口 | 程序实际接受连接的端口 |
| Probe port | 平台执行健康检查时连接的容器端口 |
| Ingress targetPort | 入口向容器转发业务请求的端口 |

本日健康端点与业务接口共用端口时，三者应一致。如果单独设置健康端口，Probe port 可以不同，但该端口必须真的有服务监听。

**容易混淆：**

- Probe 不是先访问公网 FQDN 再检查应用。
- 修改 targetPort 不会自动修改程序监听端口或自定义 Probe port。
- Probe 全通过，不代表 ingress、DNS、流量分配一定正确。

错误端口与错误路径是不同层次：

```text
错误端口 → TCP 连接失败 → 还没有机会检查 HTTP 路径

错误路径 → 端口连接成功 → 请求不存在的接口 → 返回 404 → 探针失败
```

你对错误端口的根因判断正确，但需要明确“失败发生在哪一层”，而不只说“配置错了”。

### 1.7 三类日志分别来自哪里？

| 日志 | 来源 | 主要用途 |
| --- | --- | --- |
| Console | 容器进程的 stdout/stderr | 启动输出、C# 异常、应用日志 |
| System | ACA 平台 | 镜像拉取、Revision 创建、容器退出、探针事件 |
| HTTP | Ingress 层 | 请求状态码、延迟、重试等 |

**容易混淆：**

你知道 Console Log 来自程序，但不清楚它与输出通道的关系：

```csharp
Console.WriteLine("Started");       // stdout：标准输出
Console.Error.WriteLine("Failed");  // stderr：标准错误输出
```

`ILogger` 配置了 Console 日志提供程序时，也会写入控制台输出。只写到容器文件里的日志，不会自动变成 ACA Console Log。

HTTP 日志默认关闭，需要在 Environment 的 Azure Monitor Diagnostic settings 中启用并配置目标；它不是应用异常日志。[日志分类](https://learn.microsoft.com/en-us/azure/container-apps/logging)

### 1.8 Revision 状态是线索，不是根因

- `Provisioning`：平台仍在准备和验证该版本，不等于已经发生故障。
- 长时间未完成或失败：结合 System Log 判断卡在哪一步。
- 镜像已拉取，不代表程序成功启动。
- 程序在运行，不代表 Readiness 成功。
- 探针通过，也不代表外部访问正常。

**容易混淆：**

你曾把 `ContainerCrashing` 称为“容器本身有问题”。更准确是：**容器内的主进程在启动或运行期间退出／崩溃。**

原因可能是未处理异常、启动命令、配置错误或内存问题。镜像能拉取，只排除了“拉不到镜像”，没有排除“镜像里的程序有问题”。

## 2. 核心机制与关系

### 2.1 生命周期与排障顺序不是一回事

理解系统时，可以使用：

```text
镜像可获取
→ 容器启动
→ 应用运行并监听
→ 探针判断健康和就绪
→ 入口转发业务请求
```

但实际排障不要每次机械地从镜像查起：

```text
异常现象
→ 确认目标 Revision 和时间范围
→ 查看 Revision 状态 + System Log
→ 根据证据选择分支
```

| 证据 | 下一步 |
| --- | --- |
| `ErrImagePull` | Registry 地址、镜像/tag、拉取身份权限、网络 |
| `ContainerCrashing` | Console Log、退出码、命令、配置、资源情况 |
| Probe Failed | 探针类型、端口、路径、返回码、超时及应用状态 |
| 内部健康但外部失败 | ingress、targetPort、流量分配、DNS、网络与访问限制 |

**容易混淆：**

你曾把排查排成固定直线，并把 Probe 放在容器状态之前。正确方法是**先收集证据，再分支**，不是背一个新的固定顺序。

### 2.2 两种日志要配合看

```text
System：ContainerCrashing
→ 平台观察到进程反复退出

Console：ArgumentNullException
→ 应用记录了具体异常线索
```

此时应检查异常对应的代码和配置，不要先改探针。

但 Console 也不保证总有根因：进程可能来不及输出，或日志已丢失，需要结合退出事件和资源指标。

### 2.3 Portal 诊断工具怎么选？

入口：**Container App → Diagnose and solve problems**。

| 问题 | 诊断器 |
| --- | --- |
| 镜像无法拉取 | Image Pull Failures |
| 进程退出／重启 | Container Exit Events |
| 探针失败 | Health Probe Failures |
| 入口端口疑似错误 | Ingress Port Settings Check |
| 网络或名称解析 | Networking and DNS Configuration |
| 新旧版本表现不同 | Revisions Comparison |

单个 App 出问题，先看 App 级诊断；多个 App 同时异常，再考虑 Environment 级诊断。[诊断工具说明](https://learn.microsoft.com/en-us/azure/container-apps/diagnose-solve)

**容易混淆：**

Ingress 出问题，不等于第一步就看 HTTP Log。先核对入口配置、targetPort 和对应诊断结果；请求已到入口、需要分析 5xx 或延迟时，HTTP Log 更有帮助。

## 3. 健康端点与探针配置

### 3.1 C# 三个端点的职责

| 端点 | 建议检查内容 |
| --- | --- |
| `/health/startup` | 必要初始化是否完成 |
| `/health/live` | 应用自身是否仍能响应 |
| `/health/ready` | 初始化完成，并具备必要业务服务条件 |

可以使用 ASP.NET Core Health Checks 注册检查，再按标签分别映射：

```csharp
builder.Services.AddHealthChecks();
// 在此注册实际检查，并分别标记 startup、live、ready 标签。
```

```csharp
app.MapHealthChecks("/health/startup", new HealthCheckOptions
{
    Predicate = check => check.Tags.Contains("startup")
});

app.MapHealthChecks("/health/live", new HealthCheckOptions
{
    Predicate = check => check.Tags.Contains("live")
});

app.MapHealthChecks("/health/ready", new HealthCheckOptions
{
    Predicate = check => check.Tags.Contains("ready")
});
```

需要引入 `Microsoft.AspNetCore.Diagnostics.HealthChecks`。

**注意：**这只是映射示意。没有注册匹配检查时，端点可能直接返回成功，不代表真的检查了初始化或数据库。端点不要输出密钥，也不要重定向到登录页。[ASP.NET Core 实现方式](https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/health-checks)

### 3.2 ACA YAML 探针片段

将以下内容放到目标容器的 `properties.template.containers[].probes` 下，保留原有镜像、资源和环境变量配置。

示例假设应用监听 `8080`，时间参数需要按真实启动耗时调整：

```yaml
probes:
  - type: Startup
    httpGet:
      path: /health/startup
      port: 8080
      scheme: HTTP
    periodSeconds: 5
    timeoutSeconds: 2
    failureThreshold: 24

  - type: Liveness
    httpGet:
      path: /health/live
      port: 8080
      scheme: HTTP
    periodSeconds: 10
    timeoutSeconds: 2
    failureThreshold: 3

  - type: Readiness
    httpGet:
      path: /health/ready
      port: 8080
      scheme: HTTP
    periodSeconds: 5
    timeoutSeconds: 2
    failureThreshold: 3
```

三个路径必须由应用实际提供；修改探针属于容器模板变更，会产生新 Revision。

## 4. 必须掌握的命令

> 以下采用 PowerShell 单行写法，中文参数为占位符。操作时注意选择发生故障的 Revision，不要误看旧版本日志。

### 4.1 查看版本与副本

```powershell
az containerapp revision list -n 容器应用名称 -g 资源组名称 -o table

az containerapp revision show -n 容器应用名称 -g 资源组名称 --revision 修订版本名称 -o yaml

az containerapp replica list -n 容器应用名称 -g 资源组名称 --revision 修订版本名称 -o table
```

作用：确认目标版本的配置、状态及副本情况。

关键参数：`--revision` 指定具体版本，避免混淆“最新创建”和“当前接流量”的版本。

### 4.2 查看 System Log

```powershell
az containerapp logs show -n 容器应用名称 -g 资源组名称 --type system --tail 50 --follow
```

作用：查看拉镜像、启动、探针等平台事件。

**容易混淆：**你在命令题漏了 `--type system`。默认日志类型是 console，不能省略这个参数。[日志流命令](https://learn.microsoft.com/en-us/azure/container-apps/log-streaming)

### 4.3 查看 Console Log

```powershell
az containerapp logs show -n 容器应用名称 -g 资源组名称 --type console --tail 50 --follow
```

多版本、多副本时：

```powershell
az containerapp logs show -n 容器应用名称 -g 资源组名称 --type console --revision 修订版本名称 --replica 副本名称 --container 容器名称 --tail 50 --follow
```

作用：查应用启动输出、真实监听端口、异常和检查失败原因。

关键参数：`--follow` 持续查看，按 `Ctrl+C` 退出。实时日志流不是永久历史档案。

### 4.4 核对探针和入口配置

```powershell
az containerapp revision show -n 容器应用名称 -g 资源组名称 --revision 修订版本名称 --query properties.template.containers -o yaml

az containerapp show -n 容器应用名称 -g 资源组名称 --query properties.configuration.ingress -o yaml
```

作用：第一条核对该版本的镜像、容器和 probes；第二条核对 targetPort、入口范围和流量配置。

注意：输出可能含普通环境变量，分享前检查是否有敏感内容。

### 4.5 应用修正后的配置或镜像

```powershell
az containerapp update -n 容器应用名称 -g 资源组名称 --yaml 配置文件路径

az containerapp update -n 容器应用名称 -g 资源组名称 --image 正确镜像地址
```

作用：分别修正模板配置、错误镜像地址。

关键参数：YAML 必须保留目标应用需要的配置，不能把上面的 `probes` 片段当成完整应用文件直接提交。

如果只需要修正入口端口：

```powershell
az containerapp ingress enable -n 容器应用名称 -g 资源组名称 --type 入口类型 --target-port 应用实际监听端口
```

作用：更新 ingress；入口类型替换为 `internal` 或 `external`。这不会替你修正自定义探针端口。

### 4.6 验证端点和业务接口

```powershell
curl.exe -i "https://应用FQDN/health/startup"
curl.exe -i "https://应用FQDN/health/live"
curl.exe -i "https://应用FQDN/health/ready"
curl.exe -i "https://应用FQDN/实际业务路径"
```

作用：查看状态码与响应；`-i` 显示响应头。

注意：这验证的是经过 ingress 的请求，不等同于平台直接探测容器。internal 应用应从允许访问的位置验证，不能把本机访问失败直接当作探针失败。

## 5. 我的薄弱知识点总结

重点复习：

1. **Console Log = stdout + stderr**，不是所有日志文件都会自动被收集。
2. **查看 System Log 必须指定 `--type system`**。
3. **ACA 支持 HTTP、HTTPS、TCP 探针**，不能漏掉 HTTPS。
4. **Readiness 不只检查初始化**，还持续判断当前是否能服务。
5. **根据 Revision 状态和 System Log 分支排查**，不机械地从镜像查到底。
6. **ContainerCrashing 是进程退出的症状**，不是已经确定根因。
7. **错误端口与错误路径发生在不同层次**。
8. **三类探针分开，是因为失败后的处理方式不同**，不只是为了管理方便。
9. **应用健康但访问失败，不只检查 external**，还要看 targetPort、流量、DNS 和网络限制。

第2题看错选项不计为概念错误；你已经正确掌握的慢启动重启循环、镜像拉取检查方向和诊断器匹配，不重复列为薄弱点。

## 6. 第27天最终复习清单

- [ ] 我能解释三类探针的用途与失败含义。
- [ ] 我知道默认 TCP 探针能检查什么、不能检查什么。
- [ ] 我能配置三个实际有效的 C# 健康端点及对应 ACA 探针。
- [ ] 我能区分应用监听端口、Probe port 和 ingress targetPort。
- [ ] 我能选择 Console、System、HTTP 日志，并定位到正确版本。
- [ ] 我能根据证据选择镜像、进程、探针或入口诊断分支。
- [ ] 我能解释错误镜像、错误探针端口、错误探针路径分别失败在哪一层。
- [ ] 我在实验环境中每次只制造一种错误，记录证据后恢复，再进行下一种实验。
- [ ] 修复后，我同时验证新版本状态、探针和实际业务请求，而不只看命令成功。
