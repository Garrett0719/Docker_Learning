# 第九天：容器网络与服务发现

> 学习目标：能区分容器端口与宿主机端口；理解 bridge、NAT 和 Docker DNS；能通过容器名访问同网络服务；知道 `EXPOSE` 不等于公开端口；能在目标容器重建后继续通过名称访问。

## 一、先记住结论

```text
宿主机访问容器：宿主机端口 → -p → 容器端口
容器访问同网络容器：容器名/网络别名 + 容器端口
EXPOSE：只声明预期端口，不负责发布
Docker DNS：名称 → 当前容器 IP
NAT：转换网络地址，不负责名称解析
localhost：永远表示当前网络环境中的“自己”
```

例子：

```powershell
docker run -p 5000:8080 my-api
```

表示：

```text
Host localhost:5000
        ↓
Container :8080
        ↓
Kestrel 必须确实监听 0.0.0.0:8080
```

`-p` 不会修改应用监听端口。若 Kestrel 监听 `8080`，却写成 `-p 5000:3000`，流量会进入无人监听的容器 `3000`，请求失败。

## 二、六个容易混淆的概念

| 概念 | 通俗解释 | 示例 |
| --- | --- | --- |
| Container Port | 容器内应用真正监听的端口 | Kestrel `8080` |
| Host Port | Docker 宿主机对外提供的端口 | Host `5000` |
| `-p` / `--publish` | 建立宿主机到容器的端口发布 | `-p 5000:8080` |
| `EXPOSE` | 镜像元数据，说明应用预计使用哪个端口 | `EXPOSE 8080` |
| Docker DNS | 在自定义网络中把容器名或别名解析为当前 IP | `redis → 172.x.x.x` |
| Network Interface | 网络接口，不是端口 | `eth0`、`lo` |

不要把 `8080` 叫“接口”。它是**端口**；`eth0` 才是网络接口。

## 三、`EXPOSE` 不等于公开端口

```dockerfile
EXPOSE 8080
```

它只表达：

> 这个镜像里的应用预计使用 `8080`。

它不会：

- 自动创建宿主机端口映射。
- 自动让 Kestrel 监听 `8080`。
- 决定同网络容器能不能访问该端口。

真正发布端口要用：

```powershell
docker run -p 5000:8080 my-api
```

不指定宿主机 IP 时，发布端口通常绑定到宿主机所有地址，可能被外部访问。只供本机测试时可限制到回环地址：

```powershell
docker run -p 127.0.0.1:5000:8080 my-api
```

## 四、Bridge 网络

Bridge 可以简单理解为 Docker 主机内部的一台虚拟交换机：

```text
api ─────┐
redis ───┼── app-net（bridge）── Docker Host
nginx ───┘
```

Docker 会为网络准备 Subnet 和 Gateway，并给每个连接到网络的容器分配 IP。

### 默认 bridge 与自定义 bridge

| 对比项 | 默认 `bridge` | User-defined bridge |
| --- | --- | --- |
| 创建方式 | Docker 自动创建 | `docker network create app-net` |
| 容器名自动解析 | 默认不支持 | 支持名称和网络别名解析 |
| 网络隔离 | 所有未指定网络的容器容易混在一起 | 只让需要通信的容器加入同一网络 |
| 运行中连接/断开 | 不灵活 | 支持 `network connect/disconnect` |
| 项目建议 | 不作为项目首选 | 普通单机项目优先使用 |

Bridge 主要用于**同一台 Docker 主机**。跨 Docker 主机不能只靠普通 bridge，应使用 Overlay、Kubernetes 网络或其他跨主机方案。

## 五、容器间通信为什么不用 `-p`

两个容器都在 `app-net`：

```powershell
docker network create app-net
docker run -d --name api --network app-net my-api
docker run -d --name redis --network app-net redis
```

API 应访问：

```text
redis:6379
```

而不是：

```text
localhost:6379       # 表示 api 容器自己
172.18.0.3:6379      # 写死了可能变化的 IP
redis:宿主机端口      # 同网络通信应使用容器端口
```

同一个自定义 bridge 中，容器可以直接访问对方的容器端口，不要求目标服务使用 `-p`。`-p` 主要用于宿主机、外部客户端或其他网络访问容器。

如果两个容器没有共同网络，名称通常不能直接解析，网络也不互通。可把运行中的容器接入网络：

```powershell
docker network connect app-net api
```

## 六、Docker DNS 与临时 IP

自定义网络中的容器使用 Docker 内置 DNS。`/etc/resolv.conf` 中通常可以看到：

```text
nameserver 127.0.0.11
```

访问过程：

```text
api 请求 http://test2:80
          ↓
Docker DNS 解析 test2
          ↓
得到 test2 当前 IP
          ↓
访问 test2:80
```

容器删除重建后 IP 可能改变。因此代码应该依赖稳定的容器名或网络别名，而不是写死 IP。

名称能够继续使用的前提是：

- 新容器仍使用相同名称或相同网络别名。
- 调用方与目标容器仍在同一个 User-defined network。
- 目标服务正在监听正确的容器端口，通常监听 `0.0.0.0`，而不是只监听容器自己的 `127.0.0.1`。

## 七、NAT 是什么

DNS 和 NAT 不要混淆：

```text
DNS：名称 → IP
NAT：转换源地址、目标地址或端口
```

典型出站过程：

```text
Container 172.18.0.2
          ↓ masquerading / NAT
Docker Host 192.168.1.100
          ↓
Internet
```

外部服务通常看到宿主机地址，而不是容器的 bridge IP。端口发布也会使用主机防火墙和 NAT/PAT 规则，把 Host Port 的流量转到 Container Port。

## 八、课程第 3、13、14 集：从 Kubernetes 核心概念到 Service

这三集可以按下面的顺序理解：

```text
第 3 集：Kubernetes 管理什么
    ↓
第 13 集：Pod 之间怎样通信、怎样限制通信
    ↓
第 14 集：怎样给不断变化的 Pod 提供稳定入口
```

### 1. 第 3 集：Kubernetes 核心概念

#### Cluster、Control Plane 与 Node

- **Cluster（集群）**：一组共同工作的机器，由控制平面和一个或多个工作节点组成。
- **Control Plane（控制平面）**：接收操作请求、保存集群状态、调度 Pod，并持续纠正实际状态。
- **Node（节点）**：真正运行 Pod 的机器，可以是物理机或虚拟机。
- **kubelet**：运行在 Node 上，负责保证分配到本节点的 Pod 正常运行。
- **容器运行时**：在 Node 上实际启动和管理容器。

不必背完所有组件名，先记住：**控制平面负责决定和管理，Node 负责运行。**

#### Pod 不是容器的别名

- Pod 是 Kubernetes 中最小的部署单位。
- 一个 Pod 可以有一个或多个紧密配合的容器；多数 Web API 是一个主容器一个 Pod。
- 同一个 Pod 内的容器共享网络环境，可以通过 `localhost` 通信，并共享该 Pod 的 IP 和端口空间。
- Pod 可以被删除和重新创建，新 Pod 的 IP 可能改变，因此不应把 Pod IP 写死。

```text
Node
└─ Pod 10.244.1.8
   ├─ API 容器
   └─ 辅助容器（可选）
```

#### Kubernetes 对象与期望状态

Kubernetes 通过 API 对象描述要管理的资源。常见 YAML 至少包含：

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-api
spec:
  replicas: 3
```

- `spec`：你希望系统达到的状态，例如运行 3 个副本。
- `status`：系统当前的实际状态。
- 控制器会持续比较两者；少了 Pod 就补建，多了就缩减。
- `kubectl` 本质上也是在调用 Kubernetes API。

#### Deployment、Label、Selector 与 Service

- **Deployment**：管理一组 Pod，支持副本数、故障补建和滚动更新，常用于无状态 API。
- **Label**：贴在资源上的键值标签，例如 `app: order-api`。
- **Selector**：根据标签找到目标资源。
- **Service**：为一组会变化的 Pod 提供稳定的名称和访问入口。

最小关系如下：

```text
Deployment → ReplicaSet → Pods
                           ↑
                    Label / Selector
                           ↑
Client → Service ──────────┘
```

注意：Namespace 主要用于资源分组和名称隔离，**不等于自动完成网络隔离**；限制 Pod 间通信通常要使用 NetworkPolicy，并且网络插件需要支持它。

### 2. 第 13 集：Kubernetes 网络概念及策略控制

先掌握四条通信链路：

1. **同一 Pod 内的容器之间**：共享网络环境，可使用 `localhost`。
2. **Pod 到 Pod**：通常直接使用 Pod IP 通信，具体网络由 CNI 网络插件实现。
3. **Pod 到 Service**：通过稳定的 Service 名称或虚拟 IP 访问一组后端 Pod。
4. **集群外到 Service**：根据需要使用 `NodePort`、`LoadBalancer` 或 Ingress 等方式进入集群。

NetworkPolicy 用于声明允许哪些 Pod 进入或发出流量。它类似网络层的白名单，但只有网络插件支持时才会真正生效。

### 3. 第 14 集：Kubernetes Service

Docker 与 Kubernetes 的共同思想是：

> 实例和 IP 可以变化，但调用入口尽量稳定。

```text
Docker：     api → test2:80 → Docker DNS → test2 当前 IP
Kubernetes：client → order-api:8080 → Service → 当前可用 Pod
```

更准确的 Kubernetes 链路是：

```text
Service Name
    ↓ Cluster DNS
Service（稳定入口）
    ↓ selector
匹配带有指定 Label 的 Pod
    ↓
EndpointSlice 记录当前后端地址
```

Pod 删除重建后 IP 可以变化，Service 会继续维护当前后端，客户端不需要保存 Pod IP。Service 也不会根据 HTTP 业务内容判断“哪个 Pod 更擅长处理”。

Service 常见类型：

| 类型 | 主要用途 |
|---|---|
| `ClusterIP` | 只在集群内部提供访问，默认类型 |
| `NodePort` | 在每个 Node 上开放一个端口 |
| `LoadBalancer` | 通常通过云平台负载均衡器暴露服务 |

端口不要混淆：

```yaml
ports:
  - port: 80         # 客户端访问 Service 的端口
    targetPort: 8080 # Pod 内应用监听的端口
```

这与 Docker 的 `-p 5000:8080` 有相似之处，但不是同一套机制。

### 4. 本课程的观看范围

- 第 3 集：`Kubernetes 核心概念`（28:28）。
- 第 13 集：`Kubernetes 网络概念及策略控制`（16:43）。
- 第 14 集：`Kubernetes Service`（26:30）。

第九天不需要深入安装集群、存储、调度算法或编写控制器；能讲清 **Pod 会变化，Service 提供稳定入口，Label/Selector 连接两者** 即可。

## 九、实践与验收

以下示例假设 API 镜像名为 `day9-api`，容器内监听 `8080`。

### 1. 给 API 增加测试接口

```csharp
using System.Net;

builder.Services.AddHttpClient();

app.MapGet("/network-test", async (IHttpClientFactory factory) =>
{
    var ips = (await Dns.GetHostAddressesAsync("test2"))
        .Select(x => x.ToString());

    var html = await factory.CreateClient()
        .GetStringAsync("http://test2:80");

    return Results.Ok(new
    {
        Target = "test2:80",
        ResolvedIps = ips,
        Reached = html.Contains("Welcome to nginx")
    });
});
```

重新构建为 `day9-api` 后开始实验。

### 2. 创建网络并运行两个容器

```powershell
docker network create lab-net

docker run -d --name test2 --network lab-net nginx:alpine

docker run -d --name api1 --network lab-net `
  -p 127.0.0.1:5000:8080 day9-api

Invoke-RestMethod -Uri 'http://localhost:5000/network-test'
```

预期：API 返回 `Reached: true`，并显示 `test2` 当前 IP。注意 `test2` 没有使用 `-p`。

### 3. 用 inspect 验证网络和 IP

```powershell
docker network inspect lab-net

docker inspect api1 `
  --format '{{json .NetworkSettings.Networks}}'

docker inspect test2 `
  --format '{{json .NetworkSettings.Networks}}'
```

重点看：

- `lab-net` 的 Driver、Subnet、Gateway。
- 网络 `Containers` 列表是否同时包含 `api1` 和 `test2`。
- 两个容器的 `NetworkSettings.Networks` 和 `IPAddress`。

可在非精简镜像中查看 DNS：

```powershell
docker exec api1 cat /etc/resolv.conf
```

预期能看到 `nameserver 127.0.0.11`。如果镜像没有 shell 或 `cat`，直接用 `/network-test` 返回的解析结果验证 DNS 即可。

### 4. 删除并重建目标容器

```powershell
docker inspect test2 `
  --format '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}'

docker rm -f test2
docker run -d --name test2 --network lab-net nginx:alpine

docker inspect test2 `
  --format '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}'

Invoke-RestMethod -Uri 'http://localhost:5000/network-test'
```

验收结论：

- 新旧 IP 可能相同，也可能不同，Docker 不保证重建后固定不变。
- 无论 IP 是否变化，只要名称和共同网络不变，API 仍应通过 `test2:80` 访问成功。

### 5. 验收记录表

| 检查项 | 结果 |
| --- | --- |
| `lab-net` Driver | 待记录 |
| Subnet / Gateway | 待记录 |
| `api1` IP | 待记录 |
| `test2` 重建前 IP | 待记录 |
| `test2` 重建后 IP | 待记录 |
| `/etc/resolv.conf` DNS | 待记录 |
| 重建前 API 调用 | 成功 / 失败 |
| 重建后 API 调用 | 成功 / 失败 |

## 十、常见故障排查顺序

```text
1. 两个容器是否真的在同一个自定义网络？
2. 调用地址是否为“目标名称:容器端口”？
3. Docker DNS 是否能解析目标名称？
4. 目标应用是否监听正确端口和 0.0.0.0？
5. 是否误用了 localhost 或宿主机端口？
6. 只有宿主机/外部访问失败时，再检查 -p 和防火墙。
```

`docker images` 只能查看镜像，不能证明容器连接了哪个网络。

## 十一、错题与不完整答案解析

| 题目 | 错误或遗漏 | 正确理解 |
| --- | --- | --- |
| 第 13 题：验证网络信息 | 漏选 `NetworkSettings.Networks` | `docker inspect <容器>` 的该字段能看到容器加入的网络及 IP；网络 inspect 的 `Containers` 能看到网络成员 |
| 第 16 题：综合多选 | 漏选“`-p` 主要解决 Host/外部访问容器” | 同网络容器直接使用名称和容器端口，不要求目标容器发布 Host Port |
| 第 20 题：概念解释 | 把 `8080/5000` 说成“接口” | 它们是端口；`eth0`、`lo` 才是网络接口 |
| 第 20 题：Service 链路 | 只说 Service Name 根据 selector 选择 Pod 地址 | 更准确是 DNS 解析 Service 名；Service selector 匹配 Pod，控制器维护 EndpointSlice，流量再到当前后端 |
| 第 23 题：名称稳定 | 没有说明适用前提 | 重建后继续用名称访问，要求新容器保持同名或同网络别名，并和调用方位于同一自定义网络 |

今天没有把核心原理答反，主要问题是两次漏选和术语精度。

## 十二、最终检查清单

- [ ] 能解释 `-p 5000:8080` 的左右两边。
- [ ] 知道 `EXPOSE` 只是元数据，不负责发布或监听。
- [ ] 能区分 Port 与 Network Interface。
- [ ] 能区分 DNS 与 NAT。
- [ ] 能说明为什么项目应使用 User-defined bridge。
- [ ] 知道同网络容器使用名称和容器端口，不需要 `-p`。
- [ ] 知道 `localhost` 在不同容器中表示不同的“自己”。
- [ ] 能用 `network inspect` 和 `docker inspect` 验证网络及 IP。
- [ ] 已验证目标容器删除重建后，API 仍能通过名称访问。
- [ ] 能用稳定入口解释 Docker DNS 与 Kubernetes Service 的共同思想。
- [ ] 能解释 Cluster、Control Plane、Node、Pod 的关系。
- [ ] 能说明 `spec` 是期望状态，`status` 是当前状态。
- [ ] 能说出 `Deployment → ReplicaSet → Pod` 的基本关系。
- [ ] 能说明 Label/Selector 如何把 Service 与 Pod 关联起来。
- [ ] 能区分 Service 的 `port` 与 Pod 的 `targetPort`。

## 参考资料

- [Docker networking overview](https://docs.docker.com/engine/network/)
- [Docker bridge network driver](https://docs.docker.com/engine/network/drivers/bridge/)
- [Dockerfile `EXPOSE`](https://docs.docker.com/reference/dockerfile/#expose)
- [Docker port publishing and mapping](https://docs.docker.com/engine/network/port-publishing/)
- [阿里云：CNCF x Alibaba 云原生技术公开课路线](https://developer.aliyun.com/learning/roadmap/cloudnative)
- [Kubernetes Components](https://kubernetes.io/docs/concepts/overview/components/)
- [Kubernetes Objects](https://kubernetes.io/docs/concepts/overview/working-with-objects/)
- [Kubernetes Pods](https://kubernetes.io/docs/concepts/workloads/pods/)
- [Kubernetes Deployments](https://kubernetes.io/docs/concepts/workloads/controllers/deployment/)
- [Kubernetes Service](https://kubernetes.io/docs/concepts/services-networking/service/)
- [B 站：Kubernetes 云原生技术课程（阿里云 & CNCF）](https://www.bilibili.com/video/BV1Kz411i788/)
