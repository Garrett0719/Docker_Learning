# 第二十四天：容器配置、CPU／内存、sidecar 与 init container

> 本日暂无答题记录，不设错题部分。只保留必备知识和验收命令。命令中的中文参数使用前替换；示例不代表已经部署，执行更新可能产生新副本和费用。

## 1. 配置先找对位置

| 位置 | 放什么 | 修改后的主要影响 |
| --- | --- | --- |
| `properties.template.containers` | 主容器和 sidecar 的镜像、命令、环境变量、资源等 | 有实际版本级变更时创建新 revision |
| `properties.template.initContainers` | 启动前执行的初始化容器 | 创建新 revision |
| `properties.template.scale` | 副本范围和缩放规则 | 创建新 revision |
| `properties.configuration` | ingress、流量、Secret 定义、注册表认证等 | 应用级配置，不因此创建新 revision |

**看修改属于哪里，不是看你用了 CLI、Portal 还是 YAML。**同一个镜像不变，只改 CPU、内存或容器环境变量，也会产生新 revision。[版本级与应用级变更](https://learn.microsoft.com/en-us/azure/container-apps/revisions#change-types)

ACA YAML 不是 Kubernetes Deployment YAML；这里的容器资源写成 `resources.cpu`、`resources.memory`，不直接照搬 Kubernetes 的 `requests`／`limits` 两套字段。[ACA 配置规范](https://learn.microsoft.com/en-us/azure/container-apps/azure-resource-manager-api-spec)

## 2. 容器模板里需要会读的字段

| 字段 | 作用 |
| --- | --- |
| `name` | 容器名字，用于区分主容器和其他容器，不是 App 名字 |
| `image` | 完整镜像地址，使用明确版本 tag 或 digest |
| `command` | 覆盖镜像的启动入口 |
| `args` | 启动参数列表 |
| `env` | 环境变量列表，普通值用 `value`，敏感值用 `secretRef` |
| `resources` | 容器的 CPU／内存资源配置 |

不需要改入口时，保留镜像原来的启动方式，不必重复写 `command`／`args`。需要覆盖时，用数组拆开程序与参数，例如：

```yaml
command: ["dotnet"]
args: ["实际程序集.dll", "--urls", "http://0.0.0.0:8080"]
```

这不等于把整条命令放进一个字符串。`&&`、管道等 shell 语法不会自动执行；确有需要时必须显式调用镜像中存在的 shell。[容器配置](https://learn.microsoft.com/en-us/azure/container-apps/containers#configuration)

### 环境变量与 Secret 要分开理解

```yaml
env:
  - name: App__Message
    value: "Hello from day24"
  - name: Secrets__ApiKey
    secretRef: api-key
```

- `api-key` 是 App 中已定义的 Secret 名，不是密码本身。
- 改 `env` 的值或 Secret 引用，属于 template 变更，会创建新 revision。
- 只改被引用的 Secret 值，是应用级变更，不创建新 revision；已有 revision 需要重启才能读取更新值。
- C# 默认配置可用 `configuration["App:Message"]` 读取 `App__Message`，双下划线是 .NET 的层级映射。

不要把真实凭据填进 `value` 后提交 Git。[环境变量](https://learn.microsoft.com/en-us/azure/container-apps/environment-variables)、[Secret 更新](https://learn.microsoft.com/en-us/azure/container-apps/manage-secrets)

## 3. CPU／内存：看单容器，也看整体

```yaml
resources:
  cpu: 0.5
  memory: 1Gi
```

表示为这个容器配置 `0.5 vCPU` 和 `1 GiB` 内存，不是整个 App 所有副本合计的资源。扩为多个副本后，每个副本都有对应配置；配置值也不是当前实时使用量。

Consumption 的资源组合有限制，常见组合有：

| vCPU | 内存 |
| --- | --- |
| `0.25` | `0.5Gi` |
| `0.5` | `1Gi` |
| `1.0` | `2Gi` |

这只是常见组合，不是完整上限表。当前官方文档中，普通 Consumption 在 workload profiles 环境可到 `4 vCPU / 8Gi`，旧 Consumption-only 环境上限为 `2 vCPU / 4Gi`；其他 profile 的限制另看规格。

有 sidecar 时，要检查容器的合计资源，而不是每个容器单独合法就算通过。例如主容器 `0.25 / 0.5Gi` 加 sidecar `0.25 / 0.5Gi`，合计为 `0.5 / 1Gi`。加入 init container 后也要核对平台资源校验，不能把初始化资源视为免费。[资源组合要求](https://learn.microsoft.com/en-us/azure/container-apps/containers#vcpu-and-memory-allocation-requirements)、[profile 规格](https://learn.microsoft.com/en-us/azure/container-apps/workload-profiles-overview)

## 4. Sidecar 与 init container：不要为了多容器而多容器

| 对比 | Sidecar | Init container |
| --- | --- | --- |
| YAML 位置 | 与主容器同在 `containers` 数组 | `initContainers` 数组 |
| 运行时机 | 与主容器一起运行，持续辅助 | 普通容器启动前执行，成功退出后才继续 |
| 适合用途 | 持续刷新共享缓存、转发共享文件中的日志 | 下载启动数据、生成配置、准备共享目录 |
| 不适合用途 | 需要独立发布、独立扩缩容的业务服务 | 永不退出的后台服务 |

多个 init 按定义顺序执行。一个 init 一直失败或不退出，会阻止后面的 init 和主容器启动；不能让它等待同一副本中尚未启动的主容器提供接口。[多容器与初始化](https://learn.microsoft.com/en-us/azure/container-apps/containers#multiple-containers)

### 同一个副本中共享什么？

- 主容器和 sidecar 共享网络，可通过 `localhost:端口` 通信，监听端口不能冲突。
- 文件共享需要挂载同一个卷，不是自动共享彼此的容器可写层。
- `EmptyDir` 可在同一副本的容器间共享，副本被替换后不保留；不是永久存储。[存储范围](https://learn.microsoft.com/en-us/azure/container-apps/storage-mounts)

### 适用边界

- sidecar 会随副本一起扩缩容，不能独立增加数量；各容器仍有各自进程和退出状态。
- 普通容器之间不应依赖固定启动先后，需用就绪判断或重试处理依赖。
- API 与需要独立维护的数据服务，通常应拆成不同 App 或使用托管服务，不应仅为了 localhost 通信就绑在一起。
- init 面向每个副本的初始化，新副本或失败重试都可能再次执行；“可重复执行而不破坏结果”很重要。
- 数据库迁移不能默认放进 init 就安全：多个副本可能并发迁移。应设计锁与幂等，或由受控发布任务／Job 执行。

另外，当前 Dedicated 和旧 Consumption-only 环境中的 init container，不能在运行时访问托管身份；不要把“平台能拉取 init 镜像”误认为“init 进程必然能用身份调用 Azure 服务”。[init 限制](https://learn.microsoft.com/en-us/azure/container-apps/containers#init-containers)

## 5. 实践：导出 YAML，修改并验证 revision

本次只改 CPU／内存和一个非敏感环境变量，不必为了验收额外加 sidecar 或 init。

### ① 记录更新前的版本并导出

```powershell
az account show -o table
az containerapp show -n 容器应用名字 -g 资源组名字 --query properties.latestRevisionName -o tsv
az containerapp revision list -n 容器应用名字 -g 资源组名字 -o table
az containerapp show -n 容器应用名字 -g 资源组名字 -o yaml | Out-File -FilePath 导出文件路径 -Encoding utf8
```

记录旧 revision 名称。另存一份工作 YAML 再编辑，原始导出留作对照；不要覆盖已有文件。导出可能包含敏感环境变量，分享或提交前必须检查。

### ② 保留原配置，只改目标字段

下面只是定位字段的片段，**不要拿它直接覆盖整个导出文件**：

```yaml
properties:
  template:
    containers:
      - name: 原容器名字
        image: 原镜像地址
        resources:
          cpu: 0.5
          memory: 1Gi
        env:
          - name: App__Message
            value: "Hello from day24"
```

若原来已是 `0.5 / 1Gi`，选另一个平台允许的组合，确保确实产生变化。

编辑时注意：

- 保留其他容器、环境变量、探针、挂载、缩放规则、身份和注册表配置。
- `containers`、`env` 是列表，不要假定只写一个条目会自动补回其余条目。
- 原导出带有状态信息；工作文件按 ACA 配置规范整理，移除 `systemData`、部署状态、自动生成的版本名/FQDN 等返回字段，保留需要管理的配置。
- 文件中的 App 名称、环境引用应与目标一致；真实 Secret 不写回版本库。

### ③ 使用 YAML 更新

```powershell
az containerapp update -n 容器应用名字 -g 资源组名字 --yaml 工作YAML文件路径
```

使用 `--yaml` 时，以文件内容为准，不要再混加 `--cpu`、`--memory` 等选项期待覆盖文件。CLI 文档说明 YAML 模式会忽略其他配置参数。[YAML 更新参数](https://learn.microsoft.com/en-us/cli/azure/containerapp#az-containerapp-update)

### ④ 验证新版本及实际配置

```powershell
az containerapp show -n 容器应用名字 -g 资源组名字 --query properties.latestRevisionName -o tsv
az containerapp revision list -n 容器应用名字 -g 资源组名字 -o table
az containerapp revision show -n 容器应用名字 -g 资源组名字 --revision 新修订版本名字 --query properties.template.containers[].resources -o yaml
az containerapp revision show -n 容器应用名字 -g 资源组名字 --revision 新修订版本名字 --query "properties.template.containers[].env[?name=='App__Message']" -o yaml
```

验收看：revision 名称与之前不同，新 revision 的资源和变量与 YAML 一致。只改本地文件、不提交，不会生效；完全相同的配置重复提交，也不要把它当作必然新建版本的方法。[revision 查询](https://learn.microsoft.com/en-us/cli/azure/containerapp/revision#az-containerapp-revision-show)

再检查运行与流量：

```powershell
az containerapp replica list -n 容器应用名字 -g 资源组名字 --revision 新修订版本名字 -o table
az containerapp show -n 容器应用名字 -g 资源组名字 --query properties.configuration.ingress.traffic -o table
az containerapp logs show -n 容器应用名字 -g 资源组名字 --type system --tail 50
Invoke-RestMethod 'https://应用FQDN/实际验收接口路径'
```

“新 revision 已创建”不等于“就绪且正在接流量”。Single 模式等新版就绪再切换；Multiple 模式应确认请求确实落到新版。若 API 已实现 `/config-check`，可用它验证非敏感提示文字，别返回 Secret 内容。

## 6. 本日验收结论怎么写？

填写实际记录，不要只写“更新成功”：

| 记录项 | 填写内容 |
| --- | --- |
| 更新前 | 旧 revision 名称、原 CPU／内存、原变量值 |
| 更新后 | 新 revision 名称、新 CPU／内存、新变量值 |
| 是否新建版本 | 根据 revision 名称和列表确认 |
| 为什么 | CPU／内存和容器环境变量属于 `properties.template`，是版本级变更 |
| 是否可用 | 新版是否就绪、是否接收预期流量、接口是否返回新值 |

最终应能解释：**YAML 是提交配置的方式，template 发生了什么变化，才是是否产生新 revision 的关键。**

官方练习入口：[Deploy containers to Azure Container Apps](https://learn.microsoft.com/en-us/training/modules/deploy-containers-azure-container-apps/)，本日重点是运行配置、YAML、注册表认证与部署验证。
