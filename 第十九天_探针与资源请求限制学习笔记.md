# 第十九天：探针、资源请求与限制

> 学习目标：分清“没准备好”“需要重启”“资源不够”，并能根据事件和日志解释原因。
> 本笔记结合官方资料、你的分享对话和当前项目文件整理。实验结果按对话复盘，本次没有重新运行故障实验。

## 一、先记住这六句话

1. startup：启动完成了吗？给慢启动程序留时间。
2. readiness：现在能接业务请求吗？失败主要影响接流量，不直接重启容器。
3. liveness：程序是否已经异常到需要重启？连续失败达到门槛，会触发容器重启。
4. request：安排到哪个节点时，要按多少资源需求计算；不是实际用量，也不是使用上限。
5. CPU limit：CPU 使用受限时通常先变慢；memory limit：内存超限可能导致进程被杀。
6. CrashLoopBackOff 是“反复失败后，等一等再重启”，不是具体故障原因。

## 二、三类探针到底有什么区别？

探针就是 Kubernetes 定期来问应用一个问题。HTTP 探针通过访问指定地址，判断检查是否成功。

| 探针 | 通俗理解 | 连续失败达到门槛后 | 本次地址 |
|---|---|---|---|
| startupProbe | 开店准备做好了吗？ | 终止容器，按重启策略处理 | `/health/startup` |
| readinessProbe | 现在能接待客人吗？ | 标记未就绪，常规 Service 流量不再选它 | `/health/ready` |
| livenessProbe | 店员是不是卡住了，需要重新启动？ | 终止容器，按重启策略处理 | `/health/live` |

配置 startup 后，它成功之前，另外两类探针不会执行。startup 成功一次后，这次容器运行期间就不再检查 startup；liveness 和 readiness 则持续检查。容器重启后重新走 startup。

不要理解成“startup → ready → live 三个等级”。启动完成后，live 与 ready 回答的是两个不同问题。应用完全可以活着，但暂时不能接业务。依据：[Kubernetes 三类探针](https://kubernetes.io/docs/concepts/workloads/pods/probes/)。

### 为什么 readiness 不应该直接等于 liveness？

例如数据库临时断开，但 API 进程正常：

- 如果这项业务离不开数据库，readiness 可以失败，暂时不接请求。
- liveness 不应仅因为数据库断开就失败。重启 API 并不能修好数据库。
- 如果所有 API 都因为数据库故障反复重启，就把一个故障扩大成了重启风暴。

也不是“所有外部依赖都必须加入 readiness”。如果某个第三方服务不可用，但 API 仍能提供大部分功能，应考虑允许降级，而不是让全部副本都停止接流量。

补充：readiness 不是防火墙，不代表已有连接立即断开，也不能阻止别人直接访问 Pod IP。

## 三、request 和 limit：这是你最需要补强的部分

```yaml
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
```

| 配置 | 这里表示什么 | 不要误解成什么 |
|---|---|---|
| CPU request 100m | 调度按 0.1 CPU 的需求计算；CPU 争抢时也影响分配权重 | 必须一直消耗 0.1 CPU |
| CPU limit 500m | CPU 时间额度约相当于半个 CPU | 只能使用某个固定的半颗物理核心 |
| 内存 request 128Mi | 调度按 128Mi 的需求计算 | 启动时立即占满 128Mi |
| 内存 limit 512Mi | 对容器内存使用设置边界，超限可能触发 OOM | 超过后只会像 CPU 一样变慢 |

`1000m = 1 CPU`，`100m = 0.1 CPU`。CPU 的 m 是千分之一，不是内存单位。内存 `1Mi = 1024 × 1024` 字节，`1Gi = 1024Mi`。

### 1. CPU throttling：想跑快，但额度不够

例如应用高峰时需要 500m，limit 却只有 100m。它只能少获得一些 CPU 时间，响应就可能变慢。这叫 CPU throttling，可理解为“被限制运行速度”。

它不会仅因碰到 CPU limit 就直接被杀，但可能间接触发重启：

```text
CPU limit 太低 → 应用响应慢 → liveness 超时
→ 连续失败达到门槛 → 容器重启 → 启动又需要 CPU → 再次超时
```

这不是调度器在“不断给它找新节点”。要检查 CPU 限制、探针超时时间、连续失败门槛，以及健康检查自身是否太慢。

### 2. 内存 OOM：不是限速，而是可能被杀

内存持续申请、触及容器限制时，Linux 内核可能杀掉进程。容器状态中可能记录 `OOMKilled`。对于本次 Deployment，容器退出后会被重启。

不能把 request 当成限制：request 是 128Mi、limit 是 512Mi，使用到 200Mi 并不因为超过 request 就触发 OOM。

### 3. 节点看起来很空闲，为什么 Pod 仍然 Pending？

简化例子：节点可分配 2 CPU，已有 Pod 的 CPU requests 合计 1500m，只剩 500m 可以按请求量分配。新 Pod 请求 600m，就可能出现 `Insufficient cpu`，即使节点此刻实际 CPU 使用很低。

调度器需要考虑已经承诺出去的资源，不能只看此刻是否空闲。CPU request 也影响运行时 CPU 争抢权重，但不意味着保证固定实时占用。

### 4. 没写 request，不代表一定按 0 调度

如果某项资源写了 limit、没写 request，且没有其他机制提供默认 request，Kubernetes 会把对应 limit 用作 request。实际结果应查看已创建的 Pod，不能只看本地 YAML。

以上资源规则参见：[Kubernetes 资源管理](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/)。

## 四、ASP.NET Core 健康检查：应用回答，Kubernetes 决定动作

你说“这跟之前的 healthcheck 很像”，这个理解是对的。区别在于分工：应用提供检查结果；Kubernetes 按探针类型决定是否接流量、是否重启。Dockerfile 的 `HEALTHCHECK` 不会自动变成 Kubernetes 的三类探针。

你的项目目前采用的核心代码：

```csharp
using Microsoft.AspNetCore.Diagnostics.HealthChecks;

var builder = WebApplication.CreateBuilder(args);
builder.Services.AddHealthChecks();
var app = builder.Build();

app.MapHealthChecks("/health/startup", new HealthCheckOptions
{
    Predicate = _ => false
});

app.MapHealthChecks("/health/live", new HealthCheckOptions
{
    Predicate = _ => false
});

app.MapHealthChecks("/health/ready");

app.Run();
```

- `AddHealthChecks()`：注册健康检查服务。
- `MapHealthChecks()`：给健康检查注册一个 HTTP 地址，要放在 `app.Run()` 前。
- `Predicate`：选择哪些已注册的检查参与这个地址的检查。
- `Predicate = _ => false`：一个检查都不选，**不是返回检查失败**。本例能响应时，空检查集合默认返回健康。

当前代码没有注册数据库等依赖检查，因此 `/health/ready` 还不能证明数据库可用。它是本次探针实验的基础版本。

以后增加依赖检查时，可以给它们加 `ready` 标签，再筛选：

```csharp
// 前提：已注册的业务健康检查确实设置了 "ready" 标签。
app.MapHealthChecks("/health/ready", new HealthCheckOptions
{
    Predicate = check => check.Tags.Contains("ready")
});
```

仅把 URL 分成 live 和 ready，不会自动分开检查内容。ASP.NET Core 默认将 Healthy、Degraded 映射为 200，Unhealthy 映射为 503；这些映射可以修改。依据：[ASP.NET Core 健康检查](https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/health-checks)。

## 五、你的答题复盘：哪些答错，哪些只是需要补充？

以下按分享对话中的十道验收题整理，不把助手的推测当成你的错误。

| 题目 | 你的表现 | 应记住的修正或补充 |
|---|---|---|
| 第 1 题：CPU 限制与 liveness 超时 | 明确答错 | 不是剩余 CPU 不够 request，所以不断重新找节点；是低 CPU limit 可能让应用变慢，进而触发探针重启 |
| 第 2 题：内存 request、limit 与 OOM | 核心理解正确 | 本例 resources 写在容器下，限制对象是容器；超过 request 不等于 OOM |
| 第 3 题：空闲节点仍无法调度 | 回答正确 | 你已经能区分实时空闲量与已声明 requests |
| 第 4 题：数据库故障与探针 | 回答正确 | 重启 API 无法修好数据库，依赖故障不应直接触发 liveness |
| 第 5 题：启动 25 秒，只给约 10 秒 | 回答正确 | 启动窗口要覆盖合理的最慢启动时间，并留余量，不要刚好卡边界 |
| 第 6 题：已经重启 6 次 | 排查方向正确，范围略窄 | 你提出 describe 看原因；除探针和 OOM，也可能是应用自己异常退出 |
| 第 7 题：readiness 失败导致发布卡住 | 回答正确 | 新 Pod 已经 Running，只是 NotReady；保留旧副本与滚动更新策略有关 |
| 第 8 题：两个地址执行同样检查 | 设计判断正确，有术语混淆 | 你说“成功了才进入 live”；应该是 readiness 成功后进入 Ready，live 和 ready 不是先后等级 |
| 第 9 题：CPU request 与争抢 | 回答不完整 | 除调度依据外，CPU request 还影响 CPU 争抢时的权重 |
| 第 10 题：探针超时与 OOM 同时出现 | 主线正确，默认值判断有误 | 没写 request 不代表一定没有请求量；有 limit 时可能由 limit 补齐 request |

你最值得复习的是：**request 的两种作用、缺省 request 的处理，以及 CPU 变慢如何间接导致重启。** 三类探针的大方向、内存限制、调度请求，你在后面的回答中已经基本理解。

## 六、你实际遇到的故障，应该怎样理解？

### 1. readiness 写错后，为什么看到了三个 Pod？

对话中原来有两个副本，更新后新 Pod 的 readiness 返回 404。你一度把它描述为“新容器创建不起来”。

更准确的说法是：新 Pod 已创建并运行，只是未就绪。按当时的滚动更新策略，旧副本仍保留来服务，于是暂时出现“两个旧的正常，一个新的未就绪”。不是 readiness 自动把副本数改成了三。

### 2. liveness 写错后，为什么变成 CrashLoopBackOff？

你明确观察到了：`Container study-security failed liveness probe, will be restarted`，随后又观察到 CrashLoopBackOff。

这说明错误路径反复检查失败，引发重启；反复失败后，系统延长下次重启的等待时间。修正路径才是在修原因，只删除 Pod 通常会再次遇到同样问题。

### 3. startup 返回 404，能确定是旧镜像吗？

不能。404 说明这个 HTTP 请求收到了“未找到”响应。需要核对路径、端口、路由注册和运行镜像，而不是立即断言旧镜像。

对话曾把镜像缓存作为怀疑方向，随后也纠正了“直接假设代码没更新”的判断。`already present on machine` 本身只表示节点已有该镜像，不能单独证明镜像过期。

### 4. ConfigMap 写了启动延迟，为什么没作用？

你把 `STARTUP_DELAY_SECONDS: "20"` 放进 ConfigMap。这种通过环境变量注入的值，不会自动更新到已经运行的容器中。更新 ConfigMap 后，需要让新容器启动并重新读取。

还要确认程序真正读取了这个变量，且正在运行的镜像包含延迟代码。重新 apply 完全没变的 Deployment，并不一定创建新 Pod。

### 5. OOM 实验为什么先看到 Error？

你先报告 `STATUS=Error`，又明确说 Reason 也是 Error。这不能直接当作 OOM 成功。

对话提出过“分配内存的子进程被杀，主进程以 Error 退出”的可能性，但没有足够证据时，它只是猜测。后来又出现 `python3` 找不到的错误，那是启动命令与镜像不匹配，连内存实验都没开始。

最终换成 BusyBox 主进程不断扩大字符串，对话随后记录实验成功并出现 OOMKilled。这里保留的是对话中的验收结论，本次没有再次运行，也没有重新读取当时截图。

## 七、复做实验：配置、观察、解释、恢复

仅在本地学习集群操作。一次只改一个故障点，观察后恢复，不要对生产 API 做这些实验。

当前项目目录为：`E:\Docker_Learning\study_security\study_security`。下面命令默认在该目录运行，使用当前命名空间；如果你的资源在其他命名空间，要加 `-n 名称`。`<pod-name>` 是占位符，执行前替换。

### 实验 A：配置三类探针和资源

把下面片段合并到 Deployment 中目标容器下，与该容器的 `image`、`ports` 同级。它不是完整 Deployment，不能单独 apply；保留原来的 envFrom 等配置。

```yaml
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

startupProbe:
  httpGet:
    path: /health/startup
    port: 8080
  periodSeconds: 2
  timeoutSeconds: 1
  failureThreshold: 15

livenessProbe:
  httpGet:
    path: /health/live
    port: 8080
  periodSeconds: 5
  timeoutSeconds: 2
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /health/ready
    port: 8080
  periodSeconds: 3
  timeoutSeconds: 2
  failureThreshold: 2
```

- `periodSeconds`：通常隔多少秒检查一次。
- `timeoutSeconds`：单次检查最多等多久。
- `failureThreshold`：连续失败几次，才采取相应动作。
- `initialDelaySeconds`：可选，开始检查前先等多久。
- startup 的 `2 × 15` 可粗略理解为约 30 秒的容忍窗口，不是精确到秒的重启倒计时。

8080 必须是应用真正监听的端口；`containerPort: 8080` 不会自动让应用改为监听 8080。上面的资源数值用于学习，不是通用生产配置。

```powershell
kubectl apply -f .\deployment.yaml
kubectl rollout status deployment/study-security --timeout=120s
kubectl get pods -l app=study-security
```

验收：当前副本都就绪，重启次数不持续增长，并能解释每个配置的作用。探针参数细节：[配置三类探针](https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/)。

### 实验 B：故意填错路径

先只把 readiness 路径改为 `/health/ready-wrong`，apply 后观察新 Pod：

```powershell
kubectl get pods -l app=study-security -w
kubectl describe pod <pod-name>
```

预期：新 Pod `0/1 Running`，出现 Readiness 404，**不会仅因 readiness 失败而重启**。`-w` 持续观察，用 Ctrl+C 结束后再运行下一条。

恢复 readiness，再只把 liveness 改为 `/health/live-wrong`，重新 apply。预期出现 Liveness 404、重启事件和 RESTARTS 增长；反复失败可能进入 CrashLoopBackOff。故障期间 rollout 超时是预期现象，不要一直等待发布成功。

观察后恢复 `/health/live` 并 apply，再等 rollout 成功。

### 实验 C：20 秒启动延迟

你的 Program.cs 已有读取 `STARTUP_DELAY_SECONDS` 的代码，并在 `app.Run()` 前等待。

关键点：注册了路由，不等于 HTTP 服务已经开始监听。等待发生在 Run 前，因此启动期间探针可能得到连接拒绝；这与路径不存在的 404 不同。

ConfigMap 的 data 中加入或修改：

```yaml
STARTUP_DELAY_SECONDS: "20"
```

```powershell
kubectl apply -f .\configmap.yaml
kubectl rollout restart deployment/study-security
kubectl get pods -l app=study-security -w
```

选新 Pod 核对变量和日志：

```powershell
kubectl exec <pod-name> -- printenv STARTUP_DELAY_SECONDS
kubectl logs <pod-name>
```

当前镜像如无 printenv，可通过启动日志确认实际读取值。

先保留 startup 的 `periodSeconds: 2`、`failureThreshold: 15`：预期先未就绪，启动完成后就绪，startup 给了它等待时间。

再将 startup 的 `failureThreshold` 改为 3 并 apply：约 6 秒的窗口明显小于 20 秒，程序反复在启动完成前被重启。你在对话里已经观察到这种循环以及 CrashLoopBackOff。

最后恢复 threshold 为 15，延迟值改回 `"0"`。应用 ConfigMap 和 Deployment，并确保启动了读取新值的容器，再确认 rollout 成功。

### 实验 D：OOM，可选加练

验收要求是“超内存或启动延迟”，完成 C 就满足这一项。你在对话中还补做了 OOM。

项目的 `oom-demo.yaml` 使用 BusyBox，在 32Mi 内存限制下让 shell 不断扩大字符串，8Mi 是调度请求。`restartPolicy: Always` 让容器退出后再次启动，因此可能反复 OOM。不要无限放着运行。

```powershell
kubectl apply -f .\oom-demo.yaml
kubectl get pod oom-demo -w
kubectl describe pod oom-demo
kubectl logs oom-demo --previous
```

要寻找 State 或 Last State 中的 `Reason: OOMKilled`，并结合退出码和日志。137 常见于被 SIGKILL 杀掉，**不能仅凭 137 就断言 OOM**。单独的 Error、重启次数增长，也都不够。

实验结束，保存需要的证据后，删除这个专门的测试 Pod：

```powershell
kubectl delete pod oom-demo
```

这会删除测试 Pod 和后续通过它查询日志的入口；本地 YAML 文件仍保留。若同名 Pod 已存在但需要换镜像或命令，不要假设所有字段都能原地修改，应先确认它确实是可删除的实验 Pod。

## 八、必须会用的命令和作用

| 命令 | 作用与使用时机 |
|---|---|
| `kubectl get pods -l app=study-security` | 看 API 的 READY、STATUS、RESTARTS |
| `kubectl get pods -l app=study-security -w` | 连续观察变化，Ctrl+C 退出 |
| `kubectl describe pod <pod-name>` | 看当前/上次退出状态、探针配置、资源、Events |
| `kubectl logs <pod-name>` | 看当前容器日志 |
| `kubectl logs <pod-name> --previous` | 看上一次已终止容器的日志，不是所有历史日志 |
| `kubectl get events --sort-by=.metadata.creationTimestamp` | 按创建时间看命名空间事件，重点找失败原因 |
| `kubectl get pod <pod-name> -o yaml` | 看实际生效配置，包括可能补齐的 request |
| `kubectl describe node <node-name>` | 看节点可分配资源和已经申请的资源 |
| `kubectl top pod <pod-name>` | 看近期 CPU/内存用量，需要 Metrics Server |
| `kubectl apply -f .\deployment.yaml` | 提交配置；修改 Pod 模板通常触发滚动更新 |
| `kubectl rollout restart deployment/study-security` | 主动滚动重建 Pod，可用于重新读取环境变量 |
| `kubectl rollout status deployment/study-security --timeout=120s` | 等待发布结果；超时不等于自动回滚 |
| `kubectl exec <pod-name> -- printenv STARTUP_DELAY_SECONDS` | 检查容器里的指定环境变量 |
| `kubectl port-forward pod/<pod-name> 18080:8080` | 把指定 Pod 的 8080 暂时映射到本机 18080 |
| `curl.exe -i http://localhost:18080/health/live` | 另开终端查看健康端点的 HTTP 状态码 |

`kubectl top` 不直接提供 CPU throttling 证据，也可能错过瞬间内存峰值。要确认限速，应结合监控中的 CPU 限速指标；不能把“现在 CPU 用量低”当成从未被限速。

如果改了 C# 代码，还要经历“构建镜像 → 让节点获得镜像 → 更新 Deployment 镜像版本”，光改本地代码无效。使用未用过的新标签有助于避免混淆：

```powershell
# 在项目 Dockerfile 所在目录；示例标签应换成你本次未使用的版本。
docker build -t study-security:day19-probes-v1 .
minikube image load study-security:day19-probes-v1
# 再把 deployment.yaml 中 image 改成上面的版本，然后 apply。
```

`docker build` 构建应用镜像；`minikube image load` 将它加载到 Minikube 节点，不等于发布到远程镜像仓库。

## 九、排错时按这个顺序，不要只盯着 STATUS

1. 看 READY 与 RESTARTS：是没就绪，还是容器一直退出？
2. describe 看 State / Last State：最近一次退出是什么原因？
3. 看 Events：是哪类探针失败，还是 FailedScheduling？
4. 看当前日志和 `--previous` 日志：退出前程序说了什么？
5. 核对真实配置：镜像、路径、监听端口、环境变量、resources。
6. 一次改一个原因，再观察并保留结果。

| 证据 | 优先检查什么 |
|---|---|
| `FailedScheduling / Insufficient memory` | 节点可分配内存和已有 requests |
| `Readiness probe failed` | 为什么不能接流量，不要直接解释成重启原因 |
| `Liveness probe failed` 加重启事件 | 路径、超时、检查是否太重、CPU 是否受限 |
| `Startup probe failed` | 启动耗时、窗口、路径、端口 |
| HTTP 404 | 是否请求到了正确应用的正确路由 |
| connection refused | 服务是否开始监听、端口是否正确 |
| timeout | 响应太慢、资源不足或网络问题 |
| `OOMKilled` | 内存上限、峰值、泄漏或节点内存压力等上下文 |
| `Error` | 应用异常、命令失败等，需要日志进一步判断 |
| `CrashLoopBackOff` | 只是结果，继续查上面的真正原因 |

同一个 Pod 可能先因探针失败重启，后来又因 OOM 重启。Last State 通常只保留最近一次终止状态，Events 也会过期；不能根据一次 describe 把全部历史重启精确归因。

## 十、验收清单与本次整理结论

根据分享对话，你已经做过三类探针配置、错误路径观察、启动窗口过短实验；对话末尾也记录了 OOM 实验成功。概念题总体已掌握，主要补强点见第五节。

当前文件检查发现：Program.cs 仍保留三个端点和启动延迟代码；当前 deployment.yaml 已是三个副本、使用 v3，未保留三类探针和 resources。**历史做过实验，不代表当前 YAML 仍包含实验配置。** 本笔记提供了复做片段，没有替你改动应用或集群。

下次独立验收时逐项检查：

- [ ] 能解释三类探针分别失败会怎样。
- [ ] 运行中的 Pod 确实包含三类探针和 CPU/内存 request、limit。
- [ ] readiness 错误：记录 404、未就绪，确认它本身不导致重启。
- [ ] liveness 错误：记录探针失败和重启事件，然后恢复。
- [ ] 完成启动延迟或 OOM 之一，并用状态、事件、日志解释原因。
- [ ] 能说清 CPU request 不是使用上限、低 CPU limit 可能间接造成重启。
- [ ] 能说明缺少 request 时要检查实际默认值，不能直接认定为 0。
- [ ] 最后恢复正常路径和启动配置，确认发布成功、重启次数不再增长。

### 五道快速自测

1. CPU request 是 100m，没有超过 limit 时，能否使用 300m？
2. readiness 返回 503，会不会仅因此重启容器？
3. startup 已成功，数据库后来故障，会重新进入 startup 检查吗？
4. 改了 ConfigMap，旧容器环境变量为什么没变？
5. 看到 RESTARTS=8 和 Exit Code=137，能直接断言八次都是 OOM 吗？

答案：① 有资源可用时可以。② 不会。③ 不会；容器重新启动才重新走 startup。④ 环境变量在容器启动时注入，不会这样热更新。⑤ 不能，要结合退出原因、事件与日志，而且历史证据可能不完整。

## 资料入口

- [你的 ChatGPT 学习对话](https://chatgpt.com/share/6a8fed97-be4c-83ee-bd9e-e30d2bd2ba36)
- [Kubernetes：三类探针](https://kubernetes.io/docs/concepts/workloads/pods/probes/)
- [Kubernetes：资源请求与限制](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/)
- [ASP.NET Core：健康检查](https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/health-checks)
- [Kubernetes：探针配置示例与参数](https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/)
