# 第十三天：容器安全与供应链基础

> 学习日期：2026-09-06  
> 实践项目：`study_security/study_security`  
> 实践镜像：`study-security:noroot`

## 一、今天要掌握什么

今天不是要把容器变成“绝对安全”，而是：

1. 即使 API 被利用，也尽量限制攻击者能做的事情。
2. 知道镜像里有什么、有哪些已知漏洞，并能说明哪些要修、哪些暂时接受。

核心原则：

> 默认不给多余权限，只开放程序真正需要的能力；发现漏洞后先分析实际风险，再决定处理方式。

验收目标：

- API 默认以 `app` 用户运行，而不是 `root`。
- 根文件系统只读，只给 `/tmp` 单独提供临时写入空间。
- 不使用 `--privileged`。
- 删除不需要的 Linux Capability。
- 使用可信、及时更新的基础镜像。
- 生成 SBOM，并扫描 CVE。
- 记录可修复项和暂时接受风险的理由。

---

## 二、Docker 的几层安全保护

### 1. Namespace：隔离“能看到什么”

Namespace 会让容器里的进程看到一套相对独立的环境：

- PID Namespace：进程编号。
- Network Namespace：网卡、IP、路由和端口。
- Mount Namespace：挂载和文件系统视图。
- UTS Namespace：主机名。
- User Namespace：UID、GID 映射。

它主要解决“隔离”，不负责 CPU、内存限额。

### 2. Cgroup：限制“最多能用多少”

Cgroup 可以限制和统计一组进程使用的内存、CPU、磁盘 I/O 和进程数量。

```text
Namespace：你能看到什么
Cgroup：你能使用多少
```

### 3. Docker daemon：需要重点保护的管理入口

普通 Docker Engine 的 daemon 通常拥有很高的宿主机权限。`/var/run/docker.sock` 是访问 daemon 的重要入口。如果把它挂载进业务容器，容器中的进程可能借它创建高权限容器、挂载宿主机目录或操作其他容器。

因此普通 API 容器不应随便使用：

```bash
-v /var/run/docker.sock:/var/run/docker.sock
```

### 4. Capability：把 root 的大权限拆小

Linux Capability 把传统的 root 权限拆成许多较小的能力。正确思路是：

```text
先删除不需要的能力
        ↓
程序确实缺少某项能力
        ↓
只加回这一项
```

常用参数：

```bash
--cap-drop ALL
--cap-add <真正需要的能力>
```

`CAP_SYS_ADMIN` 的范围很大，要特别谨慎。不要把“程序运行失败”直接等同于“需要 `--privileged`”。

---

## 三、`USER app`、Rootless 和 User Namespace 的区别

| 做法 | 控制对象 | 主要作用 |
|---|---|---|
| Dockerfile 的 `USER app` | 容器中的应用进程 | 让 API 默认不是容器内 root |
| User Namespace / `userns-remap` | 容器 UID 与宿主 UID 的映射 | 容器内 UID 0 可以映射成宿主普通 UID |
| Rootless mode | Docker daemon 和容器 | 让 daemon 本身也不依赖宿主 root 权限运行 |

`USER app` 很重要，但它不会自动开启只读文件系统，也不会自动消除 CVE。

Rootless 的核心价值是：daemon 或运行时即使出现漏洞，其在宿主机上的权限通常也更受限制。它不是让容器拥有独立内核，也不是自动修复镜像漏洞。

> 当前环境是 Windows + Docker Desktop。Docker 官方的 `dockerd-rootless-setuptool.sh` 主要面向原生 Linux Docker Engine，今天先掌握原理，不要直接照搬到 Windows 宿主机。

---

## 四、你的 Dockerfile 做对了什么

当前核心内容：

```dockerfile
# ---------- Build ----------
FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
WORKDIR /src

COPY study_security.csproj ./
RUN dotnet restore
COPY . .
RUN dotnet publish -c Release -o /app/publish --no-restore

# ---------- Runtime ----------
FROM mcr.microsoft.com/dotnet/aspnet:10.0 AS runtime
WORKDIR /app
COPY --from=build /app/publish .
EXPOSE 8080
USER app
ENTRYPOINT ["dotnet", "study_security.dll"]
```

做对的地方：

- 多阶段构建：SDK 只留在构建阶段，最终镜像只使用 ASP.NET Runtime。
- 使用微软官方 .NET 基础镜像。
- `USER app` 位于运行阶段，最终镜像默认以 `app` 运行。
- 使用 exec-form `ENTRYPOINT`，`dotnet` 能更直接地接收停止信号。

实际检查结果：

```text
User="app"
Workdir="/app"
Entrypoint=["dotnet","study_security.dll"]
```

还要注意：

- `USER app` 只改变进程身份，不等于根文件系统只读。
- `10.0` 是会更新的标签。生产环境如果要求构建完全可重复，可以固定经过审核的 digest，但仍要定期更新。
- 如果程序需要写日志或缓存，只挂载需要写入的目录，不要把整个根文件系统改回可写。

---

## 五、只读根文件系统和单独可写目录

现有容器 `security-demo` 已实际启用：

```text
ReadonlyRootfs=true
Tmpfs={"/tmp":"rw,size=64m"}
```

对应命令：

```powershell
docker run -d --name security-demo -p 8080:8080 --read-only --tmpfs /tmp:rw,size=64m study-security:noroot
```

效果：

```text
/app、/etc、/usr 等根文件系统内容 → 只读
/tmp                              → 可写，最多 64MB
```

如果程序不需要执行 `/tmp` 中的文件，还可以用：

```powershell
--tmpfs /tmp:rw,noexec,nosuid,size=64m
```

只读根文件系统能减少程序文件和系统文件被篡改的机会，但不是完整防护。内存、可写挂载和外部服务仍可能受到影响。

---

## 六、Capability 与 `--privileged`

当前容器的实际配置：

```text
CapDrop=null
CapAdd=null
Privileged=false
SecurityOpt=null
```

这表示：

- 没有使用 `--privileged`，这一项已通过。
- 还没有显式执行 `--cap-drop ALL`。
- 还没有设置 `no-new-privileges`。

因此 Capability 是今天最明确的待补实践项。建议另起一个测试容器：

```powershell
docker run -d --name security-hardened -p 8081:8080 --read-only --tmpfs /tmp:rw,noexec,nosuid,size=64m --cap-drop ALL --security-opt no-new-privileges:true study-security:noroot
```

确认 API：

```powershell
curl.exe http://localhost:8081/
```

检查真实配置：

```powershell
docker inspect security-hardened --format "Readonly={{.HostConfig.ReadonlyRootfs}} Privileged={{.HostConfig.Privileged}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}"
```

期望看到：

```text
Readonly=True
Privileged=False
CapDrop=["ALL"]
SecurityOpt=["no-new-privileges:true"]
```

失败时先看：

```powershell
docker logs security-hardened
```

确认确实缺少某个能力后，才使用 `--cap-add <需要的能力>`。不要为了省事改成 `--privileged`。

`--privileged` 不只是“多给一点权限”。它会开放全部 Capability、宿主设备，并削弱多项默认安全限制。

---

## 七、可信基础镜像

选择基础镜像时检查：

1. 来源可信：优先官方或组织批准的镜像。
2. 只包含运行需要的内容：最终镜像不要保留 SDK、编译器和调试工具。
3. 版本明确：不要只依赖 `latest`。
4. 持续更新：定期拉取新镜像、重建并重新扫描。
5. 记录来源：生产构建可以附加 provenance，说明镜像怎样构建、用了什么基础镜像。

常用命令：

```powershell
docker build --pull -t study-security:noroot .
docker scout recommendations local://study-security:noroot
```

当前 Scout 显示基础系统为 Ubuntu 24.04，当前标签没有更合适的新标签推荐；但它也提示缺少足够精确的 provenance，所以不能把这个结果当成完整的来源证明。

需要让仓库镜像携带 SBOM 与 provenance 时，可用：

```powershell
docker buildx build --provenance=mode=max --sbom=true -t <仓库>/<镜像>:<标签> --push .
```

本地导出的 SBOM 文件不等于镜像已经携带 SBOM attestation。

---

## 八、SBOM 和 CVE 的关系

### 1. SBOM 是“成分表”

SBOM 全称是 Software Bill of Materials，记录镜像中有哪些系统包、Runtime、库和版本。它不是漏洞报告，也不会自动修复漏洞。

项目已经生成：

```text
study_security/study_security/study-security.sbom.json
```

文件格式为 SPDX 2.3，创建工具为 Docker Scout 1.24.0。文件中有 144 个 package 记录；Scout 显示 143 个已索引软件包，是因为 SPDX 文件还包含镜像本身的 DocumentRoot 记录，计数口径不同。

```powershell
# 人工阅读更方便
docker scout sbom --format list local://study-security:noroot

# 导出 SPDX JSON
docker scout sbom --format spdx --output study-security.sbom.json local://study-security:noroot
```

`local://` 表示只使用本地镜像，不去 Registry 找同名镜像。

### 2. CVE 扫描是“根据成分表匹配已知问题”

```text
镜像 → SBOM → 组件名称与版本 → 匹配漏洞数据库 → CVE 报告
```

```powershell
# 全部已知漏洞
docker scout cves local://study-security:noroot

# 只看已有修复版本的漏洞
docker scout cves --only-fixed local://study-security:noroot

# 只看暂时没有修复版本的漏洞
docker scout cves --only-unfixed local://study-security:noroot

# 只看指定严重级别
docker scout cves --only-severity critical,high local://study-security:noroot

# 摘要与基础镜像建议
docker scout quickview local://study-security:noroot
docker scout recommendations local://study-security:noroot
```

流水线中需要在发现漏洞时返回非零状态，可以了解：

```powershell
docker scout cves --exit-code local://study-security:noroot
```

在没有制定严重级别、可修复性和例外规则前，不要把所有 CVE 都当成相同的阻断条件。

---

## 九、判断 CVE 是否优先修

```text
当前版本在 Affected range 内吗？
             ↓
有 Fixed version 吗？
             ↓
应用是否使用受影响功能？
             ↓
该功能是否能被外部输入触发？
             ↓
攻击者还需要哪些权限和条件？
             ↓
成功利用后的影响是什么？
             ↓
立即修复 / 排期修复 / 暂时接受并跟踪
```

字段解释：

- `Severity`：漏洞本身的严重程度，不等于你的系统一定受到同样影响。
- `Affected range`：受影响的软件版本范围，不是“攻击范围”。
- `Installed version`：镜像中的实际版本。
- `Fixed version`：已知包含修复的版本。
- `not fixed`：暂时没有已知修复版本，不代表没有风险，也不代表不用记录。

> 扫描结果表示“这个组件版本匹配了已知漏洞”，不等于“攻击者已经拥有可利用路径”。反过来，也不能用“不一定可利用”作为不分析、不修复的借口。

---

## 十、今天对话中的错题与薄弱点

### 1. 题目结果

今天 15 道安全验收题全部答对，最后 5 项综合题方向也都正确。对话中的综合评价约为 9.3/10，掌握度约为 93%～95%。

已经掌握：

- `USER app` 降低应用默认权限。
- `USER app` 与 `--read-only` 不能互相替代。
- 根文件系统只读时可以给 `/tmp` 单独挂载 `tmpfs`。
- `--cap-drop ALL` 后按需加回符合最小权限。
- 业务容器不应为了方便调试使用 `--privileged`。
- `docker.sock` 是高风险管理入口。
- Rootless 主要缩小 daemon 失陷后的影响。
- SBOM 是成分表，CVE 扫描是漏洞匹配。
- 扫描结果不等于漏洞一定可利用。

### 2. 没有答错选择题，但有三处表达需修正

#### “设置为已读”应改为“设置为只读”

正确说法：

```text
把根文件系统设置为只读（read-only）
```

#### `Affected range` 不是“攻击范围”

```text
Affected range = 受漏洞影响的软件版本范围
```

#### “攻击面几乎为 0”太绝对

推荐改成：

> 当前未发现 API 对外暴露相关功能，利用还需要额外条件；结合现有缓解措施，暂时接受风险并持续跟踪。

### 3. 概念会了，但实践缺一项

你正确理解了 Capability，但当前容器为：

```text
CapDrop=null
```

还需要真正运行一次带 `--cap-drop ALL` 的容器并验证 API。知识点“会解释”和验收项“实际成功”要分开记录。

### 4. SBOM 文件与 SBOM attestation 要区分

```text
本地导出 SBOM JSON
≠
Registry 中的镜像携带 SBOM attestation
```

你已经完成前者。后者适合供应链策略和 CI/CD，是后续提高项。

---

## 十一、命令速查

| 命令 | 作用 |
|---|---|
| `docker build -t study-security:noroot .` | 构建镜像 |
| `docker build --pull -t study-security:noroot .` | 先尝试拉取更新的基础镜像再构建 |
| `docker image inspect study-security:noroot` | 查看镜像默认用户、入口等配置 |
| `docker run --read-only ...` | 根文件系统只读 |
| `docker run --tmpfs /tmp:rw,size=64m ...` | 给 `/tmp` 临时可写空间 |
| `docker run --cap-drop ALL ...` | 删除全部可删除的 Capability |
| `docker run --cap-add <能力> ...` | 只增加确实需要的能力 |
| `docker run --security-opt no-new-privileges:true ...` | 阻止进程获得新权限 |
| `docker inspect <容器>` | 查看容器真实运行配置 |
| `docker exec <容器> whoami` | 查看当前运行用户 |
| `docker exec <容器> id` | 查看 UID、GID 和用户组 |
| `docker scout sbom --format list <镜像>` | 查看镜像成分 |
| `docker scout sbom --format spdx --output <文件> <镜像>` | 导出 SPDX SBOM |
| `docker scout cves <镜像>` | 扫描已知 CVE |
| `docker scout cves --only-fixed <镜像>` | 只看可升级修复项 |
| `docker scout cves --only-unfixed <镜像>` | 只看暂无修复版本的项 |
| `docker scout recommendations <镜像>` | 查看基础镜像建议 |
| `docker scout policy <镜像>` | 按策略检查漏洞、非 root、批准的基础镜像等 |

`EXPOSE 8080` 只是声明预计监听的端口，不会自动发布端口。真正发布仍需：

```powershell
-p 8080:8080
```
---

## 参考资料

- [Docker Engine security](https://docs.docker.com/engine/security/)
- [Docker Rootless mode](https://docs.docker.com/engine/security/rootless/)
- [docker container run：只读文件系统、Capability 与 privileged](https://docs.docker.com/reference/cli/docker/container/run/)
- [Docker Scout](https://docs.docker.com/scout/)
- [Docker Scout SBOM](https://docs.docker.com/scout/how-tos/view-create-sboms/)
- [docker scout cves](https://docs.docker.com/reference/cli/docker/scout/cves/)
- [Docker Scout Policy Evaluation](https://docs.docker.com/scout/policy/)
