# 第五天：多阶段构建、.NET 镜像选型与构建缓存

> 学习目标：能够为 ASP.NET Core 项目编写多阶段 Dockerfile；能解释 `restore → build → publish → runtime` 各阶段的职责；能用构建日志验证缓存是否复用；能在常规、Alpine、chiseled/distroless 镜像之间做有依据的选择；能区分 Tag 与 Digest；能排查原生 `.so` 在精简镜像中的兼容性问题。

## 一、先记住整体流程

```text
源代码、项目文件
        │
        ▼
restore：解析并还原 NuGet 依赖
        │
        ▼
build：编译源码，检查编译错误，生成程序集
        │
        ▼
publish：整理可部署的最终输出
        │
        ▼
runtime：只携带运行应用所必需的内容并启动应用
```

多阶段构建允许一个 Dockerfile 使用多个 `FROM`。每个 `FROM` 开始一个新阶段，后面的阶段通过 `COPY --from=<阶段名>` 只取走需要的产物，因此 SDK、编译器、源码和中间文件不必进入最终镜像。这样既保持 Dockerfile 清晰，又能减小最终镜像和攻击面。参见 [Docker：Multi-stage builds](https://docs.docker.com/build/building/multi-stage/)。

## 二、`restore`、`build`、`publish`、`runtime` 分别做什么

| 阶段 | 主要工作 | 典型输入 | 典型输出 | 是否需要 SDK |
| --- | --- | --- | --- | --- |
| `restore` | 读取 `.csproj`、`.sln`、NuGet 配置，解析并还原依赖 | 项目文件、解决方案文件、NuGet 配置 | `project.assets.json`、NuGet 包缓存等 | 是 |
| `build` | 编译源码并进行编译期检查 | 源码、项目文件、已还原依赖 | DLL、PDB 等编译产物 | 是 |
| `publish` | 生成可部署目录，收集应用 DLL、依赖 DLL、`.deps.json`、`.runtimeconfig.json`、静态资源等 | build 产物及发布设置 | `/app/publish` 一类发布目录 | 是 |
| `runtime` | 提供运行环境并启动应用 | publish 产物 | 正在运行的应用进程 | 否 |

需要注意：

1. `dotnet build` 默认会隐式 restore；明确执行过 restore 后，使用 `--no-restore` 可避免重复工作。
2. `dotnet publish` 默认会隐式 restore 和 build。若前面已经分别完成，可使用 `--no-restore --no-build`。
3. 实际项目不一定必须保留独立的 build 阶段；`restore → publish --no-restore` 也很常见。这里分成四段，是为了清楚理解职责并方便单独调试、测试或设置构建门禁。
4. 阶段名应使用 `restore`、`build`、`publish`、`runtime` 等有意义的名字，而不是依赖 `--from=0` 之类的数字序号。

## 三、为什么要先复制项目文件再 restore

推荐顺序：

```dockerfile
COPY MyApi.csproj ./
RUN dotnet restore MyApi.csproj

COPY . .
RUN dotnet publish MyApi.csproj -c Release -o /app/publish --no-restore
```

关键原因不是“Docker 规定必须这样写”，而是**依赖声明通常比业务源码变化少**。

Docker 会按顺序检查缓存。`COPY` 所依赖的文件没有变化，并且指令也没有变化时，对应层可以复用；某层失效后，后续层通常需要重新执行。Docker 官方也建议把昂贵且较少变化的依赖安装放在前面，把经常变化的源码放在后面。参见 [Docker：Optimize cache usage](https://docs.docker.com/build/cache/optimize/) 和 [Build cache invalidation](https://docs.docker.com/build/cache/invalidation/)。

### 只修改 `.cs` 文件

```text
COPY MyApi.csproj     CACHE HIT
dotnet restore        CACHE HIT
COPY . .              CACHE MISS  ← 源码变化
build / publish       重新执行
```

### 修改 `.csproj` 或 NuGet 版本

```text
COPY MyApi.csproj     CACHE MISS   ← 依赖声明变化
dotnet restore        重新执行
COPY . .              重新执行
build / publish       重新执行
```

### 不推荐一开始就 `COPY . .`

```dockerfile
COPY . .
RUN dotnet restore
RUN dotnet publish
```

此时任意进入构建上下文的源码、日志、IDE 临时文件或本地构建产物发生变化，都可能使 `COPY . .` 失效，进而让 restore 也重跑。

### 多项目解决方案的注意点

如果 `MyApi.csproj` 引用了 `Common/Common.csproj`，restore 前必须复制**所有会影响依赖图的项目文件**，不能只复制入口项目：

```dockerfile
COPY MySolution.sln ./
COPY MyApi/MyApi.csproj MyApi/
COPY Common/Common.csproj Common/
RUN dotnet restore MyApi/MyApi.csproj
```

随后再执行 `COPY . .`。如果遗漏被引用的项目文件，restore 可能直接失败，或无法按预期利用缓存。

## 四、可直接改造的多阶段 Dockerfile

下面是单项目 ASP.NET Core Web API 模板。请把 `MyApi.csproj` 和 `MyApi.dll` 替换为实际名称；示例使用当前学习环境中的 .NET 10，项目若不是 `net10.0`，应改成与目标框架匹配的版本。

```dockerfile
# syntax=docker/dockerfile:1

ARG DOTNET_VERSION=10.0

FROM mcr.microsoft.com/dotnet/sdk:${DOTNET_VERSION} AS restore
WORKDIR /src

# 先复制依赖声明，以便源码变化时复用 restore 层
COPY MyApi.csproj ./
RUN dotnet restore MyApi.csproj

FROM restore AS build
COPY . .
RUN dotnet build MyApi.csproj \
    -c Release \
    --no-restore

FROM build AS publish
RUN dotnet publish MyApi.csproj \
    -c Release \
    -o /app/publish \
    --no-restore \
    --no-build

FROM mcr.microsoft.com/dotnet/aspnet:${DOTNET_VERSION} AS runtime
WORKDIR /app
EXPOSE 8080

COPY --from=publish /app/publish .

# .NET 8+ 官方 Linux 镜像提供名为 app 的非 root 用户。
USER app

ENTRYPOINT ["dotnet", "MyApi.dll"]
```

说明：

1. `restore`、`build`、`publish` 都基于 SDK 阶段；最终 `runtime` 重新从较小的 `aspnet` 镜像开始。
2. 最终阶段只复制 `/app/publish`，不会自动携带 SDK 阶段的源码、NuGet 缓存或编译器。
3. `USER app` 减少应用被利用后拥有的权限。应用若要写文件，必须确保目标目录对该用户可写；不要为了省事直接改回 root。
4. chiseled/distroless 最终镜像通常没有 shell，`ENTRYPOINT` 应使用 exec form，不能依赖 `sh -c`。
5. 进阶优化可以使用 BuildKit 的 NuGet 缓存挂载；若这样做，restore、build、publish 中每个需要访问 NuGet 包的 `RUN` 都应挂载同一个 `/root/.nuget/packages` 缓存，不能只在 restore 那一步挂载。

### 建议的 `.dockerignore`

```dockerignore
**/bin/
**/obj/
.git/
.vs/
.vscode/
TestResults/
artifacts/
out/
publish/
*.user
*.suo
*.log
```

`.dockerignore` 的作用是缩小 build context、减少无关变化导致的缓存失效，并降低把本地构建产物或敏感文件误复制进构建过程的风险。

## 五、怎样选择 .NET 基础镜像

### 1. 按应用类型选镜像仓库

| 应用/阶段 | 常用镜像 | 镜像提供什么 |
| --- | --- | --- |
| restore、build、publish | `mcr.microsoft.com/dotnet/sdk` | SDK、编译器、restore/build/publish 工具及 Runtime |
| ASP.NET Core Web API | `mcr.microsoft.com/dotnet/aspnet` | .NET Runtime + ASP.NET Core shared framework |
| 普通 framework-dependent Console/Worker | `mcr.microsoft.com/dotnet/runtime` | .NET Runtime，不含 ASP.NET Core shared framework |
| self-contained、Native AOT | `mcr.microsoft.com/dotnet/runtime-deps` | 运行原生/自包含程序所需的底层依赖，不含完整 .NET Runtime |

`runtime-deps` 不是“更小的普通 runtime”。framework-dependent 应用仍需要镜像提供完整 .NET Runtime，因此不能直接拿 `runtime-deps` 运行；self-contained 应用把所需 Runtime 带在发布产物中，才适合优先考虑它。参见 [Microsoft：.NET container images](https://learn.microsoft.com/en-us/dotnet/core/docker/container-images)。

### 2. 常规、Alpine、chiseled/distroless 的权衡

| 类型 | 优点 | 主要代价/风险 | 适用建议 |
| --- | --- | --- | --- |
| 常规 Debian/Ubuntu | 兼容面广；常见系统库、ICU、tzdata 和诊断方式较完整 | 体积和组件通常更多，潜在攻击面较大 | 不确定 native 兼容性时的稳妥默认选择 |
| Alpine | 通常较小；包管理体系简单 | 使用 musl 而非 glibc；第三方 native `.so` 可能不兼容；默认精简变体可能缺 ICU/tzdata | 已验证 `linux-musl-*`、native 依赖和 globalization 后使用 |
| Ubuntu chiseled / Azure Linux distroless | 只保留运行必需组件；无 shell、无包管理器；通常默认非 root；攻击面更小 | 不能临时 `apt install`；`docker exec ... sh` 可能不可用；现场排障更难；额外系统依赖要在构建时规划 | 依赖清晰、测试充分、观测完善的生产环境 |

Microsoft 的镜像变体说明指出，Alpine、Ubuntu Chiseled 等尺寸优先变体默认可能不含 ICU 与 tzdata；需要全球化和时区数据时应选择相应的 `-extra` 变体，或在适合安装软件包的镜像中显式补齐。参见 [.NET Image Variants](https://github.com/dotnet/dotnet-docker/blob/main/documentation/image-variants.md)。

### 3. “更小”不等于“必然更好”

选择基础镜像时至少同时检查：

1. 应用类型：Web、普通 Worker、self-contained 还是 Native AOT。
2. C library：native 依赖针对 glibc 还是 musl。
3. CPU 架构：`amd64`、`arm64` 等是否匹配。
4. globalization：是否使用 ICU、时区、区域性排序、大小写和格式化。
5. 系统能力：是否需要 Kerberos、LDAP、msquic、字体、图像/视频库等。
6. 运维方式：是否依赖 shell、`curl`、`ps`、`ldd` 等现场诊断工具。
7. 安全与观测：能否通过日志、指标、追踪、临时 debug 镜像或 Kubernetes ephemeral container 排障。

正确思路是：**在满足兼容性、可观测性和运维要求的前提下，选择足够小的镜像**，而不是只按镜像大小排序。

## 六、Tag 与 Digest

```text
mcr.microsoft.com/dotnet/aspnet:10.0
                                 └── Tag

mcr.microsoft.com/dotnet/aspnet@sha256:abc123...
                                 └── Digest
```

| 对比项 | Tag | Digest |
| --- | --- | --- |
| 本质 | 人类可读的名字/指针 | 基于内容的加密摘要，通常是 `sha256` |
| 是否可变 | 可被发布者重新指向新内容 | 对应内容不可变；内容变化会产生新 Digest |
| 优点 | 易读、易做版本管理；浮动 Tag 可接收补丁 | 能精确锁定构建或部署所用内容，便于复现和审计 |
| 代价 | 同一 Tag 在不同时间可能得到不同内容 | 不会自动获得新安全补丁，需要主动更新 Digest |

生产中可以同时保留可读 Tag 和固定 Digest：

```dockerfile
FROM mcr.microsoft.com/dotnet/aspnet:10.0@sha256:<完整摘要>
```

其中 Tag 便于人理解，真正锁定内容的是 Digest。Docker 官方明确说明 Tag 可以变化，Digest 是不可变引用。参见 [Docker：Pin base image versions](https://docs.docker.com/build/building/best-practices/#pin-base-image-versions) 和 [Image digests](https://docs.docker.com/dhi/explore/security-concepts/digests/)。

推荐策略：

1. 开发阶段可使用明确的主次版本 Tag，例如 `10.0`，不要把 `latest` 当成可重复构建保证。
2. 严格生产环境或供应链要求高的环境使用 Digest 固定。
3. 固定 Digest 后，通过依赖更新工具或定期流程检查新基础镜像，审核后更新 Digest 并重新构建。
4. 给自己的应用镜像同时记录语义版本、Git Commit SHA 和 Registry Digest，例如：

```text
my-api:1.4.2
my-api:git-a83f21c
my-api@sha256:...
```

## 七、构建密钥与运行时密钥

不要用下面的方式向构建过程传递私有 NuGet 密码、npm token 或 API token：

```dockerfile
ARG NUGET_TOKEN
ENV NUGET_TOKEN=$NUGET_TOKEN
```

`ARG`/`ENV` 不适合保存构建秘密，因为敏感值可能暴露在镜像配置、构建历史、缓存或构建元数据中。Docker 官方建议使用 Secret Mount，让秘密只在指定的 `RUN` 执行期间临时可见。参见 [Docker：Build secrets](https://docs.docker.com/build/building/secrets/)。

示意写法：

```dockerfile
RUN --mount=type=secret,id=nuget_config,target=/root/.nuget/NuGet.Config \
    dotnet restore MyApi.csproj
```

构建时传入：

```powershell
docker build `
    --secret id=nuget_config,src=NuGet.Config `
    -t my-api:day5 .
```

需要区分两个生命周期：

| 秘密用途 | 例子 | 正确方式 |
| --- | --- | --- |
| 构建时使用 | 私有 NuGet 源凭据、npm token、拉取私有依赖的凭据 | BuildKit Secret/SSH mount |
| 运行时使用 | 数据库密码、第三方 API Key、证书私钥 | 在部署时由 Docker/Kubernetes/云 Secret Manager 注入 |

BuildKit Secret 不会自动变成应用运行时的 Secret。反过来，也不要为了让应用运行时能读取密码，就把它在 build 阶段烤进镜像。

另一个缓存细节：Secret 的**内容变化**本身不会使构建缓存失效。如果秘密改变后必须强制相关步骤重跑，需要改变参与缓存计算的其他输入，或对对应阶段使用合适的缓存失效方式。

## 八、实践与验收

### 验收 1：把第四天的 Dockerfile 改成多阶段构建

验收标准：

- [ ] 构建阶段使用 `sdk`。
- [ ] restore 前只复制 `.csproj`、`.sln` 和其他会影响依赖图的文件。
- [ ] restore 后才复制完整源码。
- [ ] build/publish 使用 `--no-restore`；若 publish 前已 build，再使用 `--no-build`。
- [ ] 最终阶段使用 `aspnet`、`runtime` 或 `runtime-deps` 中与应用类型匹配的镜像。
- [ ] 最终阶段只 `COPY --from=publish` 发布产物。
- [ ] 使用非 root 用户运行，且写目录权限正确。
- [ ] 有 `.dockerignore`。
- [ ] `ENTRYPOINT` 使用 exec form。

### 验收 2：连续构建两次并比较耗时

PowerShell 示例：

```powershell
$first = Measure-Command {
    docker build --progress=plain -t my-api:day5 . | Out-Host
}
$first.TotalSeconds

$second = Measure-Command {
    docker build --progress=plain -t my-api:day5 . | Out-Host
}
$second.TotalSeconds
```

记录：

| 构建 | 耗时（秒） | 观察 |
| --- | ---: | --- |
| 第一次 |  | 需要拉取基础镜像并执行全部步骤 |
| 第二次 |  | 大部分步骤应显示 `CACHED`，通常明显更快 |

注意：第一次耗时可能包含网络下载，不能把所有差值都归因于 Dockerfile 优化；验收重点还要看构建日志中的 `CACHED`。

### 验收 3：修改一个 `.cs` 文件，确认 restore 被复用

1. 只改 `Program.cs` 中一个不会影响项目依赖的内容。
2. 再次执行：

```powershell
docker build --progress=plain -t my-api:day5 .
```

3. 在日志中确认类似结果：

```text
CACHED  COPY MyApi.csproj ./
CACHED  RUN dotnet restore MyApi.csproj
        COPY . .                         # 重新执行
        dotnet build / publish           # 重新执行
```

判定：

- [ ] restore 显示 `CACHED`：通过。
- [ ] `COPY . .` 与后面的 build/publish 重跑：符合预期。
- [ ] 如果 restore 重跑，检查 `.csproj`、NuGet 配置、`Directory.Build.*`、`Directory.Packages.props` 等依赖输入是否也发生了变化，或 Dockerfile 是否过早执行了 `COPY . .`。

不要使用 `--no-cache` 做这个验收，因为它会主动禁用缓存。

### 验收 4：记录最终镜像大小

```powershell
docker image ls my-api:day5 --format "{{.Repository}}:{{.Tag}}`t{{.Size}}"
docker image inspect my-api:day5 --format "{{.Size}}"
docker history my-api:day5
```

记录：

| 镜像 | 基础镜像类型 | 本地显示大小 | 备注 |
| --- | --- | ---: | --- |
| `my-api:day5` | 例如 `aspnet:10.0` |  | 多阶段最终镜像 |

`docker image ls` 显示的是本地镜像大小；Registry 传输时的压缩大小可能不同。`docker history` 可用于观察最终镜像有哪些层，并确认没有把 SDK、源码或不必要工具带进去。

### 可选加分验收

```powershell
# 验证应用能启动
docker run --rm -p 8080:8080 --name my-api-day5 my-api:day5

# 另开终端查看实际用户
docker exec my-api-day5 id
```

如果使用 chiseled/distroless，镜像可能没有 `sh` 或 `id`，不能把“无法 exec 这些命令”误判为应用故障；应使用应用日志、健康检查、镜像元数据或专用 debug 手段验证。

## 九、8 月 28 日错题解析

原始验收中选择题成绩为 8/10，错误题是第 7 题和第 10 题。原始记录见 [ChatGPT 分享会话](https://chatgpt.com/share/6a8fed97-be4c-83ee-bd9e-e30d2bd2ba36)。

### 错题 7：Alpine 下出现 `error while loading shared libraries`

题目：把普通 `aspnet:10.0` 换成 Alpine 变体后，出现原生共享库加载错误，哪些方向值得优先调查？

你的答案：`BCE`  
正确答案：`ABCE`

你漏选的是：

> A. 原生 `.so` 依赖是否存在。

#### 为什么 A 必须先查

`error while loading shared libraries` 并不自动等于“glibc 与 musl 不兼容”。动态加载器报错可能来自多层原因：

```text
目标 libExample.so 根本没有进入最终镜像
        ↓
目标库存在，但它依赖的另一份 .so 缺失
        ↓
库按 glibc 构建，而 Alpine 使用 musl
        ↓
库或应用的 CPU 架构不匹配
        ↓
第三方 NuGet 包未提供 linux-musl 对应 native asset
```

因此排查顺序建议是：

1. 确认目标 `.so` 是否被 publish 并复制进最终镜像。
2. 使用 `ldd`、`readelf -d` 等查看传递依赖；关注 `not found`。精简镜像没有这些工具时，可在同发行版的 debug 阶段检查。
3. 使用 `file`、`uname -m` 或构建元数据确认 `x64/arm64` 是否匹配。
4. 确认目标是 `linux-x64` 还是 `linux-musl-x64`，以及库针对 glibc 还是 musl。
5. 确认 ICU、tzdata、字体、图像/视频库等额外依赖是否存在。
6. 查第三方 native library 是否正式支持 Alpine；不要假定所有 Linux `.so` 都能跨发行版直接使用。

这道题的错误本质：**过早把问题归因于 Alpine 兼容性，漏掉了更基础的“文件是否存在”检查。**

### 错题 10：把宿主机发行版与容器用户态混为一谈

题目：Debian-based ASP.NET 镜像正常，换成 Alpine 后 `libExample.so` 加载失败，最不合理的第一反应是什么？

正确答案：`D`

> D. “宿主机是 Ubuntu，所以 Alpine 容器一定不能运行。”

#### 为什么 D 不合理

Linux 容器共享宿主机的 Linux Kernel，但容器携带自己的用户态文件系统和系统库：

```text
Ubuntu 宿主机的 Linux Kernel
        ├── Debian 用户态容器
        ├── Alpine 用户态容器
        └── Ubuntu 用户态容器
```

所以 Ubuntu 宿主机可以运行 Alpine 容器；宿主发行版名称不要求与容器发行版相同。真正应该优先检查的是：

- `libExample.so` 是否存在；
- 它依赖的其他 `.so` 是否存在；
- glibc/musl ABI 是否兼容；
- CPU 架构和 RID 是否匹配；
- 应用所需的内核能力是否满足。

这道题的错误本质：**没有区分“共享内核”与“容器自带用户态”。**

## 十、最后速记

1. 多阶段构建的核心是：构建工具留在前面的 SDK 阶段，最终镜像只拿发布产物。
2. `restore` 依赖项目文件；先复制项目文件再 restore，才能让单纯源码变化复用依赖层。
3. 只改 `.cs`：restore 应命中缓存；改 `.csproj`：restore 应重跑。
4. ASP.NET Core Web 用 `aspnet`；普通 framework-dependent 应用用 `runtime`；self-contained/Native AOT 可考虑 `runtime-deps`。
5. Alpine 更小但使用 musl；第三方 glibc `.so` 不能假定兼容。
6. chiseled/distroless 通过减少组件、shell 和包管理器缩小攻击面，但排障与临时安装工具更困难。
7. Tag 是可变名字，Digest 是不可变内容引用；生产锁定 Digest 后仍要建立更新流程。
8. 原生库加载失败先查“文件在不在”，再查传递依赖、libc、架构和 RID。
9. Ubuntu 宿主机可以运行 Alpine 容器；容器共享内核，但拥有自己的用户态。
10. 构建时 Secret 与运行时 Secret 属于不同生命周期；验收缓存不能只看耗时，还必须看构建日志中的 `CACHED`。

## 十一、参考资料

1. [Docker：Multi-stage builds](https://docs.docker.com/build/building/multi-stage/)
2. [Microsoft：.NET container images](https://learn.microsoft.com/en-us/dotnet/core/docker/container-images)
3. [Docker：Lab — Building Container Images](https://docs.docker.com/guides/lab-building-images/)
4. [Docker：Optimize cache usage in builds](https://docs.docker.com/build/cache/optimize/)
5. [Docker：Build cache invalidation](https://docs.docker.com/build/cache/invalidation/)
6. [Docker：Building best practices](https://docs.docker.com/build/building/best-practices/)
7. [.NET 官方仓库：Image variants](https://github.com/dotnet/dotnet-docker/blob/main/documentation/image-variants.md)
8. [.NET 官方仓库：ASP.NET Alpine 多阶段 Dockerfile 示例](https://github.com/dotnet/dotnet-docker/blob/main/samples/aspnetapp/Dockerfile.alpine)
9. [Docker：Build secrets](https://docs.docker.com/build/building/secrets/)
10. [原始 ChatGPT 分享会话](https://chatgpt.com/share/6a8fed97-be4c-83ee-bd9e-e30d2bd2ba36)
