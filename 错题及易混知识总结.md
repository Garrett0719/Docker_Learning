# 错题及易混知识总结:

## Dockerfile、镜像与容器:

1. Dockerfile 描述**如何构建镜像**，不是如何构建容器。
2. 正确链路：`Dockerfile → Image → Container`。
3. 一个容器通常包含一个或多个 Linux 进程，不能简单说“容器严格等于一个进程”。

## Dockerfile构建缓存:

错误理解：先 `COPY .csproj` 后，`dotnet publish` 就能直接使用 `--no-restore`。

正确顺序：

```dockerfile
COPY *.csproj ./
RUN dotnet restore
COPY . ./
RUN dotnet publish -c Release -o /app/publish --no-restore
```

1. `COPY . .` 放得太早，源码变化会使该层及后续缓存失效。
2. 不是“全部文件重新复制”，而是缓存失效后，后续命令需要重新执行。
3. 应配合 `.dockerignore` 排除 `bin/`、`obj/`、`.git/` 等无关内容。
4. 生产镜像推荐多阶段构建：SDK 阶段负责编译，Runtime 阶段只保留发布产物。
5. `EXPOSE` 放在 ENTRYPOINT 前后通常不是重要优化点。

## docker build中的-f与点号:

```bash
docker build -t my-api -f docker/Dockerfile .
```

1. `-f docker/Dockerfile`：指定使用哪个 Dockerfile。
2. 最后的 `.`：指定当前目录为 build context，决定 `COPY` 能访问哪些文件。
3. 当前目录中的文件就叫 `Dockerfile` 时，可以省略 `-f Dockerfile`。

## exec form与shell form:

### exec form:

```dockerfile
ENTRYPOINT ["dotnet", "App.dll"]
```

1. 直接启动程序，不自动经过 shell。
2. `dotnet` 通常直接成为 PID 1，能更直接地接收停止信号。
3. JSON 数组必须用双引号，不能写成 `['dotnet', 'App.dll']`。
4. 不自动处理 `$PORT`、`&&`、`*` 等 shell 语法。

### shell form:

```dockerfile
ENTRYPOINT dotnet App.dll
```

1. Linux 上通常相当于 `/bin/sh -c "dotnet App.dll"`。
2. shell 可能成为 PID 1，应用成为子进程。
3. shell 不一定正确转发 `docker stop` 的信号，可能影响优雅退出。

最简记忆：

```text
RUN：需要 &&、变量、管道时可用 shell form。
ENTRYPOINT / CMD：运行主程序时优先用 exec form。
```

## exec form为什么不展开环境变量:

```dockerfile
ENV PORT=8080
ENTRYPOINT ["dotnet", "App.dll", "--port", "$PORT"]
```

1. 程序可能收到字面量 `$PORT`，因为没有 shell 负责展开。
2. 最推荐让应用直接读取 `PORT` 环境变量。
3. 必须展开时可以写：

```dockerfile
ENTRYPOINT ["sh", "-c", "exec dotnet App.dll --port \"$PORT\""]
```

4. `sh -c` 负责变量展开；`exec` 用 dotnet 替换 shell，使 dotnet 成为 PID 1。

## ENTRYPOINT与CMD:

```dockerfile
ENTRYPOINT ["dotnet", "App.dll"]
CMD ["--port", "8080"]
```

1. `ENTRYPOINT`：固定“运行谁”。
2. `CMD`：提供默认命令或默认参数。
3. 默认执行：`dotnet App.dll --port 8080`。
4. `docker run my-api --port 9000`：新参数**整体替换 CMD**，最终执行 `dotnet App.dll --port 9000`。
5. `--entrypoint` 才用于覆盖 ENTRYPOINT。
6. `docker run --entrypoint /bin/sh my-api` 覆盖 ENTRYPOINT 后，原 CMD 仍可能保留，最终可能变成 `/bin/sh --port 8080` 并报错。

## 容器PID 1与docker exec:

1. 容器生命周期主要绑定创建容器时启动的初始主进程。
2. PID 1 退出，容器就会停止；不是容器内还有任意进程就继续运行。
3. `docker exec` 是在现有容器的 Namespace 和 Cgroup 中再启动一个进程，不是“打开容器本体”。
4. `docker exec` 启动的 shell 不能代替原 PID 1 维持容器生命周期。

## Kestrel的概念:

1. Kestrel 是 ASP.NET Core 内置的 Web 服务器。
2. 它负责真正监听容器内的地址和端口，并接收 HTTP 请求。
3. Docker 只负责转发请求，不会替 Kestrel 监听。

```text
浏览器 → 宿主端口 → Docker转发 → 容器端口 → Kestrel → ASP.NET Core
```

## 应用监听与Docker端口映射:

```bash
docker run -p 127.0.0.1:5000:8080 my-api
```

1. `127.0.0.1`：Docker 在宿主机哪个 IP 上接收请求。
2. `5000`：宿主机入口端口。
3. `8080`：转发到的容器端口。
4. 右边的容器端口必须和 Kestrel 实际监听端口一致。
5. 左边的宿主端口可以修改，但不能已被占用。

如果 Kestrel 监听 `8080`：

```bash
-p 5000:8080    # 可以
-p 9000:8080    # 可以
-p 5000:3000    # 通常失败，容器3000没人监听
```

## 两个0.0.0.0不要混淆:

1. `-p 0.0.0.0:5000:8080`：这里的 `0.0.0.0` 属于**宿主机**，表示在宿主全部 IPv4 接口上发布端口。
2. Kestrel 监听 `0.0.0.0:8080`：这里的 `0.0.0.0` 属于**容器 Network Namespace**，表示监听容器全部 IPv4 接口。
3. 两者处于不同网络环境，是两份独立配置。

## 127.0.0.1与0.0.0.0:

1. 容器里的 `127.0.0.1` 只代表容器自己的 loopback。
2. 如果 Kestrel 只监听 `127.0.0.1:8080`，Docker 从容器网卡转入的请求可能无法被接收。
3. 对外服务通常监听 `0.0.0.0:端口`、`[::]:端口` 或其他可达接口。
4. `[::]` 是 IPv6 unspecified address，作用类似 IPv4 的 `0.0.0.0`。

## EXPOSE与-p:

1. `EXPOSE 8080` 只是声明镜像中的应用预计使用该端口。
2. 它不会让 Kestrel 自动监听，也不会自动发布宿主端口。
3. 真正的宿主端口映射仍需使用 `docker run -p ...`。

## 没有ASPNETCORE_URLS为什么也能访问:

因为 Kestrel 的监听配置还可能来自：

1. 代码中的 `ListenAnyIP`、`UseUrls`、`app.Run(url)`。
2. `appsettings.json` 中的 Kestrel Endpoints。
3. `ASPNETCORE_HTTP_PORTS`、`ASPNETCORE_HTTPS_PORTS`。
4. 命令行参数 `--urls`。
5. 基础镜像、Docker Compose 或 Kubernetes 配置。

结论：没有显式写 `ASPNETCORE_URLS`，不代表 Kestrel 没有监听配置。

## 8080是不是Kestrel永远的默认端口:

不是。

1. `8080` 是 .NET 8 及以后官方 ASP.NET Core 容器镜像通常提供的默认 HTTP 端口配置。
2. 它不是 Kestrel 永远固定的端口，代码、配置、环境变量和命令行都可能覆盖。
3. 最终结果应查看：

```bash
docker logs 容器名
```

重点找：`Now listening on: ...`。

## 端口访问失败的排查顺序:

1. `docker ps`：确认容器是否运行，检查 `PORTS`。
2. `docker inspect 容器名`：检查映射、环境变量和启动配置。
3. `docker logs 容器名`：确认应用是否启动，以及实际监听地址和端口。
4. 容器仍运行时，用 `docker exec` 配合 `ss -lnt` 等工具查看真实监听状态。
5. 依次核对：宿主端口 → 容器端口 → Kestrel 监听端口。

不要一看到访问失败就断定是 `-p` 写错；也可能是应用没启动、监听错端口，或只监听容器的 `127.0.0.1`。

## OCI三类规范:

1. Image Spec：镜像怎样组织，包括 layer、config、manifest。
2. Distribution Spec：镜像怎样在 Registry 中 push、pull。
3. Runtime Spec：怎样根据 rootfs 和运行配置启动容器进程。

```text
Image：怎么装
Distribution：怎么运
Runtime：怎么跑
```

## OCI Image Layer与OverlayFS lowerdir:

错误理解：OCI Image Layer 就是磁盘上的 OverlayFS lowerdir。

正确链路：

```text
OCI Image Layer
    ↓
Registry中的layer blob
    ↓
pull、校验、解压、应用文件变化
    ↓
filesystem snapshot
    ↓
在OverlayFS实现中可能组织为lowerdir
```

1. OCI Layer 是镜像格式概念，描述文件的新增、修改和删除。
2. Registry 保存的是 blob，不是已经挂载好的 merged 目录。
3. OverlayFS lowerdir 是 Linux 主机上的文件系统实现概念。
4. OCI 不强制使用 OverlayFS，因此两者不能直接画等号，也不保证严格一层对一层。

## OverlayFS读写:

1. `lowerdir`：一个或多个只读基础内容。
2. `upperdir`：当前容器自己的可写变化。
3. `workdir`：OverlayFS 内部处理 copy-up、rename 等操作的工作目录，不是普通缓存层。
4. `merged`：lower 与 upper 合并后，容器看到的统一视图。
5. 读取：upper 有同路径文件时读取 upper，否则读取 lower。
6. 新建：直接写入 upper。
7. 第一次修改 lower 文件：copy-up 到 upper，再修改 upper 副本。
8. 后续修改：直接修改 upper，不再重复完整 copy-up。
9. 删除 lower 文件：通过 upper 中的 whiteout 等机制让它在 merged 中消失。
10. 多个容器可以共享镜像只读内容，但各有独立 upper，因此修改互不影响。

## 最后速记:

1. Dockerfile 构建 Image，Image 创建 Container。
2. `COPY csproj` 后还要 restore，publish 才能 `--no-restore`。
3. ENTRYPOINT 固定程序，CMD 提供可覆盖的默认参数。
4. exec form 不经过 shell，因此不自动展开 `$变量`。
5. `docker run IMAGE 参数` 覆盖 CMD；`--entrypoint` 才覆盖 ENTRYPOINT。
6. Docker `-p` 负责转发，Kestrel 负责真正监听。
7. `-p` 右侧端口必须和应用实际监听端口一致。
8. `EXPOSE` 只是声明，不等于发布端口。
9. 8080 是现代官方 .NET 容器镜像的常见默认配置，不是 Kestrel 永久固定端口。
10. OCI Layer 属于镜像格式层；OverlayFS lowerdir 属于本地文件系统实现层。

## 参考资料:

1. [原始ChatGPT分享对话](https://chatgpt.com/share/6a8fed97-be4c-83ee-bd9e-e30d2bd2ba36)
2. [Dockerfile reference](https://docs.docker.com/reference/dockerfile/)
3. [Docker端口发布](https://docs.docker.com/get-started/docker-concepts/running-containers/publishing-ports/)
4. [Microsoft：配置Kestrel端点](https://learn.microsoft.com/aspnet/core/fundamentals/servers/kestrel/endpoints)
5. [Microsoft：ASP.NET Core容器默认端口改为8080](https://learn.microsoft.com/dotnet/core/compatibility/containers/8.0/aspnet-port)
6. [OCI Image Specification](https://github.com/opencontainers/image-spec)
7. [Linux Kernel：OverlayFS](https://docs.kernel.org/filesystems/overlayfs.html)
