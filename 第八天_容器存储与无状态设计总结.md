# 第八天：容器存储与无状态设计

> 学习目标：能解释镜像只读层和容器可写层；能选择 Bind Mount、Named Volume、tmpfs；能验证删除并重建容器后的数据结果；能说明 Web API 为什么应尽量无状态。

## 一、先记住结论

```text
程序和固定依赖        → Image
允许丢失的普通临时文件 → Writable Layer
临时且尽量不落盘      → tmpfs
开发源码、宿主配置     → Bind Mount
单机容器持久数据       → Named Volume
业务数据               → Database
Session                → Redis / Database
用户上传文件           → Object Storage / 共享持久存储
密码、API Key          → Secret 管理
```

关键原则：**容器可以随时替换，重要状态不能只存在某一个 API 容器里。**

## 二、镜像只读层与容器可写层

```text
Container
┌──────────────────────────┐
│ Container Writable Layer │  每个容器独立
├──────────────────────────┤
│ Image Read-only Layers   │  多个容器可以共享
└──────────────────────────┘
```

- 镜像层是只读、不可变的，保存程序和固定运行依赖。
- `docker run` 会为每个容器增加一层独立的 Writable Layer。
- 没有挂载存储时，容器新建和修改的文件会进入自己的 Writable Layer。
- 修改镜像中已有文件时，通常会先把它复制到 Writable Layer，再修改，这叫 copy-up。
- `docker stop` 只停止容器，数据仍跟着容器存在；`docker rm` 删除容器时，Writable Layer 也被删除。
- 用同一个镜像重新创建容器，只会得到一个新的 Writable Layer，不会找回旧容器数据。

因此，两个由同一镜像启动的容器分别修改 `/app/config.json`，不会自动改到对方。

## 三、四种存储方式对比

| 方式 | 数据在哪里 | 删除容器后 | 适合场景 | 主要注意点 |
| --- | --- | --- | --- | --- |
| Writable Layer | 容器自己的可写层 | 丢失 | 可丢失的普通临时文件 | 不适合重要数据 |
| Bind Mount | 指定的宿主机路径 | 保留 | 开发源码、宿主配置、导出文件 | 依赖宿主路径；默认可写 |
| Named Volume | Docker 管理的宿主存储 | 保留 | 单机数据库等持久数据 | 不是天然的跨主机共享盘 |
| tmpfs | 主要在宿主机内存 | 停止后丢失 | 临时中间文件、减少磁盘 I/O | 占用容器内存；可能受 swap 影响 |

### 1. Bind Mount

```powershell
docker run --mount "type=bind,src=D:\Docker_Learning\data,dst=/data" day8-api
```

容器里的 `/data/test.txt` 实际对应宿主机的：

```text
D:\Docker_Learning\data\test.txt
```

特点：

- 宿主机和容器可直接读写同一批文件，很适合开发时挂载源码。
- 默认可写，容器可能修改甚至删除宿主文件；只需读取时加 `readonly`。
- 强依赖宿主机路径，换机器后相同路径可能不存在。
- 挂载到容器已有内容的目录时，原有内容会被暂时遮住，不是被删除。
- 推荐使用含义更清楚的 `--mount`。

只读示例：

```powershell
docker run --mount "type=bind,src=D:\Docker_Learning\config,dst=/app/config,readonly" day8-api
```

### 2. Named Volume

```powershell
docker volume create day8-data
docker run --mount "type=volume,src=day8-data,dst=/data" day8-api
```

- 由 Docker 创建和管理具体存储位置。
- 生命周期独立于某个容器。删除容器不会自动删除 Named Volume。
- 新容器重新挂载同名 Volume，可以继续读取原数据。
- `docker volume rm day8-data` 或清理 Volume 时，数据仍会被删除。
- 本地 Volume 通常属于某一台 Docker 主机。跨主机共享需要合适的 Volume Driver、网络存储或云存储。

所以，“Volume 更容易管理和备份”不等于“它会自动迁移到其他机器”。

### 3. tmpfs

```powershell
docker run --mount "type=tmpfs,dst=/data,tmpfs-size=64m" day8-api
```

- 数据主要放在内存中，不进入容器 Writable Layer。
- 容器停止、重启或宿主机重启后，数据不会保留。
- 适合图片处理中间文件、一次性缓存等。
- tmpfs 会计入容器内存限制；写得过多可能引发 OOM。
- Linux 可能把内存换出到 swap，因此不要把它绝对理解为“任何情况下都不会触碰磁盘”。

## 四、Bind Mount 进阶题

这部分是今天题目涉及的进阶知识，日常开发通常不需要修改默认值。

### `bind-recursive`

决定创建 Bind Mount 时，源目录中**已经存在的子挂载点**是否一起带入容器。

```text
/mnt
└─ usb/  ← 已经挂载的另一块盘
```

### `bind-propagation`

决定绑定完成后，**后来新增或移除的子挂载**是否在宿主机与容器之间传播。

| 模式 | 传播方向 |
| --- | --- |
| `rprivate` | 两边都不传播，默认值 |
| `rshared` | 双向传播 |
| `rslave` | 宿主机 → 容器；容器 → 宿主机不传播 |

挂载传播只适用于 Linux 主机，Docker Desktop 不支持该功能。

## 五、为什么 Web API 应尽量无状态

无状态不是“系统没有数据”，而是：

> 一次请求所需的重要状态，不依赖某一个特定 API 实例的本地文件。

错误设计：

```text
API-A → /data/session.json、/data/orders.db
API-B → 自己的另一份 session 和 orders
```

问题：

- API-A 删除后，本地状态丢失。
- 请求转到 API-B 时，读不到 API-A 的 Session。
- 增加 API-C 只会增加第三份不一致的数据。
- 滚动升级、故障迁移和水平扩容都会变得困难。

推荐设计：

```text
              Load Balancer
             /      |      \
          API-A   API-B   API-C
             \      |      /
              外部共享状态
        Redis / Database / Object Storage
```

| 数据 | 推荐位置 |
| --- | --- |
| Session | Redis 或数据库 |
| 订单、用户等业务数据 | PostgreSQL、MySQL 等数据库 |
| 用户上传文件 | S3、Azure Blob、MinIO 等对象存储 |
| 数据库密码、API Key | Secret Manager / 容器编排平台的 Secret |
| 临时中间文件 | tmpfs 或其他临时存储 |
| 程序、Runtime、固定 `.so`、`ffmpeg` | 镜像 |

单机环境中，上传文件放 Named Volume 可以工作；多节点环境更适合对象存储或真正支持多节点访问的持久卷。

## 六、实践与验收

以下示例假设镜像名为 `day8-api`，API 在容器内监听 `8080`。

### 1. 让 API 写文件

在 `Program.cs` 增加一个最小测试接口：

```csharp
const string filePath = "/data/test.txt";

app.MapPost("/storage/write", () =>
{
    Directory.CreateDirectory("/data");
    File.WriteAllText(filePath, DateTimeOffset.UtcNow.ToString("O"));
    return Results.Ok("written");
});

app.MapGet("/storage/read", () =>
    File.Exists(filePath) ? File.ReadAllText(filePath) : "MISSING");
```

应用使用非 root 用户时，要保证 `/data` 对该用户可写。

### 2. 测试 Writable Layer

```powershell
docker run -d --name day8-test -p 8081:8080 day8-api
Invoke-RestMethod -Method Post -Uri 'http://localhost:8081/storage/write'
Invoke-RestMethod -Uri 'http://localhost:8081/storage/read'

docker rm -f day8-test
docker run -d --name day8-test -p 8081:8080 day8-api
Invoke-RestMethod -Uri 'http://localhost:8081/storage/read'
```

预期：重建后返回 `MISSING`。

### 3. 测试 Bind Mount

```powershell
docker rm -f day8-test
New-Item -ItemType Directory -Force -Path 'D:\Docker_Learning\day8-bind-data'

docker run -d --name day8-test -p 8081:8080 `
  --mount "type=bind,src=D:\Docker_Learning\day8-bind-data,dst=/data" day8-api

Invoke-RestMethod -Method Post -Uri 'http://localhost:8081/storage/write'
docker rm -f day8-test

docker run -d --name day8-test -p 8081:8080 `
  --mount "type=bind,src=D:\Docker_Learning\day8-bind-data,dst=/data" day8-api

Invoke-RestMethod -Uri 'http://localhost:8081/storage/read'
```

预期：文件仍在，宿主目录中也能看到 `test.txt`。

### 4. 测试 Named Volume

```powershell
docker rm -f day8-test
docker volume create day8-data

docker run -d --name day8-test -p 8081:8080 `
  --mount "type=volume,src=day8-data,dst=/data" day8-api

Invoke-RestMethod -Method Post -Uri 'http://localhost:8081/storage/write'
docker rm -f day8-test

docker run -d --name day8-test -p 8081:8080 `
  --mount "type=volume,src=day8-data,dst=/data" day8-api

Invoke-RestMethod -Uri 'http://localhost:8081/storage/read'
```

预期：重新挂载同一个 `day8-data` 后，文件仍在。

### 5. 验收记录

| 方式 | 删除并重建后 | 原因 |
| --- | --- | --- |
| Writable Layer | 文件消失 | 数据属于旧容器，随容器删除 |
| Bind Mount | 文件保留 | 数据在宿主机指定目录 |
| Named Volume | 文件保留 | Volume 生命周期独立于容器 |

验收时还要确认：Bind Mount 和 Named Volume 必须重新挂载**同一个宿主路径或同一个 Volume**，新容器才能看到原文件。

## 七、错题与需要修正的表达

| 题目 | 原答案或表达 | 正确理解 |
| --- | --- | --- |
| 第 4 题：临时且尽量不落盘 | 选择 Writable Layer | Writable Layer 虽然可丢失，但通常仍经过容器文件系统；题目强调“尽量不落盘”时应选 tmpfs |
| 第 12 题：容器间文件隔离 | “每个容器就是一个进程” | 容器可以有多个进程；文件互不影响的关键是各自独立的 Writable Layer 和文件系统视图 |
| 第 14 题：Named Volume | 把优势概括成“方便数据迁移” | 更准确的是 Docker 管理存储位置，且生命周期独立于容器；跨主机迁移仍需备份、恢复或共享存储方案 |
| 第 15 题：状态外置 | 把用户数据、Session、私密数据都放 Volume | 业务数据放数据库，Session 放 Redis，上传文件放对象存储，密钥放 Secret 管理；状态外置不等于全部塞进 Volume |
| 第 17 题：Uploads | 只回答放 Volume | 单机 Volume 可以；多实例、多节点生产环境通常更适合对象存储或可跨节点访问的持久卷 |

唯一明确答错的是 tmpfs 题；其余几项主要是表达不够精确。

## 八、最终检查清单

- [ ] 能解释 Image Read-only Layer 与 Container Writable Layer。
- [ ] 知道删除容器会删除其 Writable Layer，停止容器不会。
- [ ] 能根据场景选择 Bind Mount、Named Volume 或 tmpfs。
- [ ] 知道 Bind Mount 默认可写且依赖宿主路径。
- [ ] 知道 Named Volume 不随容器自动删除，也不等于自动跨主机共享。
- [ ] 能说明 `bind-recursive` 与 `bind-propagation` 的区别。
- [ ] 能把 Session、业务数据、上传文件和 Secret 放到合适的外部系统。
- [ ] 已完成三种存储方式的删除重建实验并记录结果。

## 参考资料

- [Docker Storage](https://docs.docker.com/engine/storage/)
- [Docker bind mounts](https://docs.docker.com/engine/storage/bind-mounts/)
- [Docker volumes](https://docs.docker.com/engine/storage/volumes/)
- [Docker tmpfs mounts](https://docs.docker.com/engine/storage/tmpfs/)

